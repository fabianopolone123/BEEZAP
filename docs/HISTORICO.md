# Histórico

Registro curto das alterações feitas no projeto.

## 2026-06-25
- Criação da estrutura inicial do projeto Django.
- Criação da tela inicial de login com layout base.
- Organização dos arquivos soltos em `assets/`.
- Geração da versão transparente do logo.
- Ajustes visuais na tela de login, incluindo slogan e link de recuperação.
- Criação do `README.md` e do `.gitignore`.
- Inicialização do repositório Git e push para o GitHub.
- Criação do histórico de alterações e do arquivo de padrões do projeto.
- Início da autenticação com usuário por e-mail e perfis de acesso.
- Criação da dashboard inicial com layout didático e botões por nível de usuário.
- Remoção do texto explicativo do cartão de perfil no dashboard.
- Remoção do bloco de perfil logado da barra lateral no dashboard.
- Etapa 1 concluída: criada documentação inicial do projeto.
- Etapa extra concluída: criada documentação de regras Git em `docs/GIT.md`.
- Etapa documental concluída: removidos arquivos de plano/contexto duplicados e mantido `docs/HISTORICO.md` como registro oficial do que já foi feito e decidido.
- Configuracao inicial da W-API adicionada para ADM, com armazenamento de instance ID/token e base tecnica de envio de texto sem expor token na interface.
- Rota oficial de envio de texto da W-API confirmada e aplicada no cliente para instancias Lite usando `POST /send-text` com `instanceId` em query string.
- URL oficial de envio da W-API ajustada para `POST /v1/message/send-text` com `instanceId` em query string e retorno estruturado com `messageId` e `insertedId`.
- Tela de configuracao da W-API reorganizada em um unico card com duas colunas responsivas para configuracao e teste de envio.
- Ajuste visual dos alertas da tela W-API para mensagens mais compactas e simples para o usuario final.
- Notificacoes do sistema convertidas para toast compacto reaproveitavel no template base.
- Reduzido o espacamento vertical entre o titulo e o card principal da tela W-API.
- Ajustado o gap da pagina W-API para aproximar o card principal do cabecalho.
- Tela W-API passou a usar CSS especifico em `static/css/wapi_settings.css`.
- Documentados os padroes de interface, CSS por pagina e notificacoes toast no projeto.
- Ajustado o topo da tela W-API para reduzir espacos vazios acima do titulo e antes do card.
- Tela W-API passou a compactar o proprio container `dashboard-main` com classe especifica.
- Compactacao adicional do container W-API com reducao do padding superior e do espaco entre titulo e card.
- Corrigido o esticamento vertical do grid da tela W-API para manter titulo e card agrupados no topo.
- Criada a tela de atendentes para ADM com cadastro, edicao em modal e vinculo de usuario com senha inicial 1234.
- Implementada troca obrigatoria de senha inicial para atendentes no primeiro acesso.
- Removida a barra piscando de digitacao em elementos nao editaveis, preservando campos reais de formulario e documentando a regra de UX.
- Implementada recuperacao de senha por codigo de 6 digitos enviado via WhatsApp, com hash, expiracao e limite de tentativas.
- Restaurado o visual discreto do botao de recuperacao de senha na tela de login.
- Melhorada a experiencia da tela de login com olhinho para revelar a senha ao pressionar e indicador visual de carregamento no botao Entrar.
- Corrigido o contraste do texto do botao Entrar e refinado o visual discreto do olhinho da senha.
- Ajustada a area clicavel do olhinho da senha para evitar caret piscando ao clicar na lateral do icone.
- Ajustado o caret dos campos da tela de login para nao ficar piscando em foco parado, mantendo indicacao discreta durante a digitacao.
- Restaurado o caret normal dentro dos campos editaveis de e-mail e senha na tela de login.
- Ajustado o clique na area vazia final dos campos de e-mail e senha para nao posicionar o caret no fim do texto.
- Corrigido o cursor indevido na tela de login com regra de caret limitada a elementos nao editaveis e sem interferir nos campos reais.
- Ajustado o clique na sobra final do campo de senha para nao posicionar o cursor piscando fora do texto digitado.
- Ampliada a area do olhinho da senha para cobrir toda a lateral direita do campo e evitar cursor piscando nessa regiao.
- Reestruturado o campo de senha do login para separar input e botao do olhinho, evitando sobreposicao e cursor indevido na lateral.
- Ajustado o clique na sobra vazia do campo de e-mail do login para nao posicionar cursor fora do texto ou placeholder.
- Reestruturado o campo de e-mail do login para separar area editavel e sobra visual, evitando cursor piscando na lateral vazia.
- Criada tela visual de Conversas com layout em 3 colunas (lista, chat, informacoes do contato), dados ficticios, animacao de digitacao e CSS especifico em static/css/conversations.css.
- Criada tela de Setores com model Sector (ManyToMany com Attendant), drag and drop nativo, modal de cadastro/edicao/exclusao, alternativa de adicao via botao para mobile, salvamento via AJAX e CSS especifico em static/css/sectors.css.
- Corrigida criacao de setor: substituidas variaveis JSON com auto-escaping do Django (que convertia aspas para &quot; quebrando JSON.parse) pelo filtro json_script, resolvendo o SyntaxError que impedia o registro de todos os event listeners incluindo o botao Novo setor.
- Atendentes alocados em setores agora somem da lista de disponíveis (ao arrastar, ao adicionar via modal mobile e ao carregar a pagina); reaparecem ao serem removidos do setor quando não estão em mais nenhum outro.
- Preparada a base tecnica de IA local com Ollama em modo de teste, tela de Automacao para ADM e fallback seguro sem envio pelo WhatsApp.
- Criado cadastro administrativo de regras de atendimento para orientar a IA, com regras salvas no banco e busca simples de regras relevantes.
- Tela de teste da IA passou a permitir selecionar setor, usar regras cadastradas e exibir regras encontradas sem acionar W-API ou WhatsApp.
- Configurado o cliente Ollama para usar CPU por padrao (`OLLAMA_NUM_GPU=0`), evitando erro CUDA no teste local da IA.
- Aplicadas regras padrao editaveis para orientar o teste da IA em atendimento, encaminhamento, horario, financeiro, vendas e suporte.
- Preparado webhook publico da W-API para registrar mensagens recebidas com parser defensivo, token separado e visualizacao dos ultimos eventos na tela de configuracao.
- Preparado o projeto para deploy de homologacao em VPS Linux com settings por ambiente, exemplos de systemd/Nginx, requirements e documentacao de deploy.
- Melhorado o teste de recebimento da W-API para usar janela real de 1 minuto com polling, sem criar evento falso.
- Corrigido o recebimento de mensagens da W-API: webhook agora aceita a chamada externa quando nenhum token de webhook esta configurado (antes era recusado com 403 em producao), mantendo a validacao apenas quando o token existe.
- Webhook publico passou a responder tambem em `/beezap/webhook/wapi/` alem de `/webhook/wapi/`, e a tela W-API passou a exibir a URL sob o prefixo `/beezap/` para configurar na W-API.
- Adicionados logs seguros no recebimento do webhook, sem gravar token nem payload bruto e com telefone mascarado.
- Adicionado botao "Testar recebimento" na tela W-API que registra um evento de exemplo interno, sem chamar a W-API nem enviar mensagem pelo WhatsApp.
- Corrigida a exibicao de mensagens reais recebidas pela W-API: removidos os botoes de teste ("Testar recebimento" e "Iniciar teste de recebimento") que confundiam o usuario, e a tela passou a mostrar "Aguardando novas mensagens" com a lista "Ultimos eventos recebidos" atualizando sozinha a partir dos eventos reais salvos no banco.
- Parser do webhook da W-API ampliado para entender formatos aninhados reais (por exemplo `messages[]`, `message.conversation`, `message.extendedTextMessage.text`, `key.remoteJid`, `pushName`, `contact`), com telefone normalizado sem sufixo de JID e sem quebrar em payloads desconhecidos.
- Ajustada a orientacao da tela para usar a URL publica sob `/beezap/webhook/wapi/` no campo "Ao receber uma mensagem" da W-API, sem citar localhost, ngrok ou tunel.
- Parser da W-API reforcado com busca recursiva defensiva (fallback) que procura mensagem, telefone e nome em qualquer profundidade do payload, alem dos caminhos fixos, resolvendo eventos que chegavam como `webhookReceived` com os campos vazios.
- Telefone passou a ser normalizado apenas com digitos (DDI+DDD+numero), removendo sufixos como `@s.whatsapp.net`/`@c.us` e simbolos, e ignorando valores curtos invalidos.
- Adicionado log seguro de diagnostico no webhook que registra apenas os nomes das chaves do payload (nunca valores nem token) para ajudar a mapear o formato real da W-API.
- Tela W-API passou a exibir no maximo os 5 eventos mais recentes em "Ultimos eventos recebidos".
- Corrigida a extracao do telefone do webhook da W-API: prioridade para o remetente real em `data.key.participant`/`remoteJid`, `data.from`, `data.sender`, `chatId`, `senderNumber` e afins, com fallback recursivo, ignorando grupos (`@g.us`) e mantendo intactas a extracao de mensagem e nome.
- Ajustada a extracao do telefone ao formato real da W-API Lite (evento `webhookReceived`): passa a ler o remetente em `sender.id`, ignora o numero conectado (`connectedPhone`) e os identificadores internos `@lid`, mantendo mensagem e nome intactos.
- Conversas passaram a usar dados reais do banco: criados os models `Contact`, `Conversation` e `Message`, e um service central em `wapi/services.py` para criar contato/conversa/mensagem.
- Webhook da W-API agora cria/atualiza contato, conversa e mensagem recebida (apenas texto, ignorando mensagens enviadas pelo proprio numero), sem quebrar o recebimento se a integracao falhar.
- Tela Conversas deixou de usar dados ficticios: lista conversas reais ordenadas pela ultima mensagem, com estado vazio amigavel; abrir uma conversa carrega mensagens reais via AJAX e zera as nao lidas.
- Envio de texto pela propria conversa usa a W-API ja configurada, salva a mensagem enviada (`out/sent`) e atualiza o resumo; nao envia mensagem vazia e desabilita o botao durante o envio; erro amigavel via toast.
- Preparada a base de transferencia de atendimento: conversa pode receber setor e atendente responsavel por endpoint/select simples na tela; anexos nao foram implementados nesta etapa.
- Adicionado comando `sync_wapi_events_to_conversations` para transformar eventos W-API ja recebidos em conversas/mensagens reais.
- Corrigido o envio de texto pela tela Conversas reutilizando o mesmo servico e a mesma configuracao da tela de teste da W-API (Instance ID e Token salvos), sem logica paralela.
- Envio centralizado passou a normalizar o telefone (apenas digitos, sem `+`, espacos, parenteses, `@s.whatsapp.net`/`@c.us`) e a registrar log seguro do motivo real de falha da W-API (status e corpo, nunca o token), facilitando o diagnostico.
- Mensagens de erro do envio ficaram amigaveis e especificas: configuracao ausente, contato sem telefone ou falha de conexao; nunca expondo token, payload ou traceback. Mensagem so e salva como enviada quando a W-API confirma sucesso.
- Corrigido o layout da tela Conversas para manter a barra de digitacao fixa no rodape do chat: aplicado `min-height: 0` na cadeia grid/flex (pagina, corpo, colunas, painel e area de mensagens) e `minmax(0, 1fr)` na linha do grid, de modo que apenas a lista de mensagens e a lista lateral rolem internamente, sem empurrar o composer para fora da tela; ajuste apenas no CSS especifico `conversations.css`.
- Corrigido o composer que ficava colado na ultima mensagem: elementos com `[hidden]` (placeholder do chat, painel, estados vazios) eram sobrepostos por `display: flex/grid` da classe e continuavam ocupando espaco `flex: 1`; adicionada regra `.conversations-page [hidden] { display: none !important; }` para realmente ocultar, deixando o painel do chat preencher toda a altura e a barra de digitar fixa no rodape. Ajuste verificado por render headless.
- Adicionado versionamento (`?v=`) nos links de CSS da tela Conversas para forcar o navegador a baixar o CSS atualizado, evitando que ajustes visuais fiquem presos em cache antigo.
- Implementados filtros reais na lista de Conversas (Todas, Nao lidas, Em atendimento, Aguardando, Finalizadas) com contadores reais do banco: novo endpoint `conversas/lista/` retorna conversas filtradas por status + contagens; a busca por nome/telefone/ultima mensagem funciona combinada com o filtro ativo; a lista e recarregada via AJAX sem recarregar a pagina, atualizando contadores ao abrir/ler conversa e periodicamente. Reaproveitado o CSS de chips ja existente; composer e recebimento/envio nao afetados.
- Corrigida de forma definitiva a publicacao de estaticos no VPS (CSS/JS que nao apareciam apos deploy): `STATIC_URL` passou a vir de variavel de ambiente (padrao `/static/`, producao usa `/beezap/static/` no `.env`), evitando editar `settings.py` a mao no servidor; `STATICFILES_DIRS` mantido com `static/` e aviso para nunca esvaziar. Criados `deploy/deploy.sh` (git pull + migrate + collectstatic + restart), atualizado o exemplo de Nginx com a opcao recomendada (servir a pasta-fonte `static/` no prefixo `/beezap/`, com o admin vindo do collectstatic) e criado `docs/DEPLOY.md` documentando problema, causa, correcao, fluxo de deploy e como testar. Fim da necessidade do `cp -r static/* staticfiles/` manual.
- Preparada a compatibilidade de midia da W-API no BEEZAP respeitando o plano LITE: model `Message` ganhou `message_type` e campos de midia (`media_file`, `media_url`, `media_mimetype`, `media_status`) sem perder o historico antigo (default `text`); parser detecta o tipo real (texto/imagem/audio/video/documento/sticker/gif/reacao/localizacao/contato/desconhecido) e metadados; client W-API centralizado com `send_image/audio/video/document` e `download-media` (bodies LITE confirmados); webhook passou a criar a mensagem por tipo, baixando e salvando a midia recebida localmente (com estados carregando/indisponivel) e rotulos amigaveis na ultima mensagem; chat renderiza cada tipo (imagem/audio/video/documento/gif/sticker/reacao/aviso) sem quebrar o composer fixo nem os filtros. `MEDIA_URL` tambem passou a vir do `.env` (`/beezap/media/`). Envio de reacao/sticker/gif e recurso PRO e ficou bloqueado (apenas exibido quando recebido). UI de anexo para enviar midia LITE ficou como proxima etapa. Documentado em `docs/WAPI_MEDIA_INTEGRATION.md`.
- Adicionada a UI de anexos no composer da tela Conversas para enviar midia LITE (imagem/audio/video/documento): botao de clipe com menu; endpoint `conversas/<id>/enviar-midia/` valida tipo/mimetype/tamanho (`WAPI_MEDIA_MAX_MB`, padrao 16 MB), salva o arquivo em `MEDIA/whatsapp/outgoing/` com nome unico, monta a URL publica (respeitando `/beezap/media/`) e envia pela W-API via client; mensagem enviada e salva (com `external_message_id`) e aparece na hora no chat, com rotulo na ultima mensagem. Em falha da W-API a mensagem fica como `failed/unavailable` e mostra toast amigavel, sem quebrar a tela. Texto, filtros, recebimento de midia e composer fixo permanecem intactos.
- Corrigido o erro "Nao foi possivel abrir a conversa" no VPS: como o app roda sob o prefixo `/beezap/` mas o Django gera `{% url %}` sem o prefixo, as chamadas AJAX da tela Conversas (abrir conversa, enviar, filtros, transferir, enviar midia) iam para `/conversas/...` e caiam em outro site no Nginx. O JS passou a montar a base das rotas a partir do caminho real da pagina no navegador (`window.location.pathname` ate `/conversas/`), funcionando com ou sem prefixo.
- Ajuste global do prefixo `/beezap/`: adicionado `FORCE_SCRIPT_NAME` via `.env` (ex.: `/beezap`), fazendo o Django gerar todas as URLs (links da barra lateral, redirects de login) com o prefixo; `LOGIN_URL`/`LOGIN_REDIRECT_URL`/`LOGOUT_REDIRECT_URL` passaram a ser nomes de rota (herdam o prefixo). A URL do webhook exibida na tela agora vem de `reverse('wapi-webhook')` (com prefixo vira `/beezap/webhook/wapi/`). Corrigido tambem um erro de sintaxe de template introduzido no commit anterior (o texto literal de uma tag de URL dentro de um comentario JS quebrava a renderizacao da tela Conversas).
- Adicionada gravacao de audio pelo navegador no composer: botao de microfone inicia a gravacao (`MediaRecorder`) com indicador e cronometro; clicar de novo para e envia, e o ✕ cancela. O audio gravado (ogg/opus ou webm/opus conforme o navegador) e enviado pelo mesmo fluxo de midia LITE (`enviar-midia/`, `media_type=audio`). Requer HTTPS; documentada a possivel necessidade de converter webm para mp3/ogg no futuro.
- Corrigido o envio de audio: a W-API so aceita `.mp3`/`.ogg` e o Chrome grava `.webm`; o backend passou a converter o audio recebido para `.ogg` (opus) com ffmpeg antes de enviar (com erro amigavel se o ffmpeg nao estiver instalado). Requer `MEDIA_URL=/beezap/media/` no `.env` para a W-API conseguir baixar o arquivo publico.
- Documentadas as dependencias e variaveis novas: `ffmpeg` como dependencia de sistema (nota no `requirements.txt`, secao em `docs/DEPLOY.md` e aviso no `deploy/deploy.sh`), e as variaveis obrigatorias do `.env` em producao (`FORCE_SCRIPT_NAME=/beezap`, `STATIC_URL=/beezap/static/`, `MEDIA_URL=/beezap/media/`).
- Criado `docs/CONTEXTO.md` (handoff completo: visao geral, arquitetura, models, W-API LITE/PRO, tela Conversas, deploy/armadilhas do VPS, variaveis, comandos de diagnostico e pendencias) e atualizado o `docs/INDEX.md` para listar todos os documentos.
- Corrigido o play de audio/midia RECEBIDA que nao tocava: o download local do arquivo falhava e o sistema guardava so o link remoto da W-API (que expira), deixando o `<audio>` apontando para um link morto. `_download_to_media_file` ficou robusto (2 tentativas, timeout 60s, User-Agent de navegador, recusa de resposta HTML/JSON que nao e midia) e passou a logar o motivo exato da falha (sem expor link/token). Adicionados `retry_media_download()`/`retry_incoming_media_downloads()` e o comando `retry_wapi_media` para recuperar midias antigas que ficaram sem arquivo local (o download-media gera um fileLink novo a cada chamada). Envio de audio nao foi afetado (ja salvava local).
- Configurado `LOGGING` no `settings.py`: sem ele, os logs `INFO` da aplicacao (`beezap.*`: webhook, envio, midia) nao apareciam no journal (o padrao do Django so emite `WARNING+` para loggers nao-`django`). Agora vao para o stdout/stderr capturado pelo systemd/journald em nivel `INFO` (ajustavel por `BEEZAP_LOG_LEVEL`), tornando uteis os logs `[WAPI WEBHOOK]` e `download-media`.
- Melhorado o play de audio recebido no chat: o `<audio>` passou a informar o `type` (mimetype) e `preload=metadata`, com fallback "Baixar audio" quando o navegador nao consegue tocar (causa comum: servidor entrega o arquivo com content-type generico, ou o link remoto expirou). Ampliado o mapa mimetype->extensao (m4a/aac/amr/wav/webm/3gp/mov) para o arquivo salvo ter a extensao certa, e adicionado log seguro em `download-media` (tipo/mimetype/extensao/resultado local-ou-remoto, sem expor link/token) para diagnosticar no VPS.
- Nome real do grupo (em vez de "Grupo <jid>"): o webhook LITE quase sempre traz so o JID, entao o BEEZAP passou a buscar o nome via `GET /v1/group/get-all-groups` da W-API. Novo `get_all_groups()` em `wapi/client.py` (com helper GET `_wapi_get`), e em `wapi/services.py` `resolve_group_name()`/`sync_group_names()` + parser defensivo do retorno (lista/dict aninhado, id em `id`/`groupId`/`remoteJid`/`jid`, nome em `name`/`subject`/`title`). Na criacao de uma conversa de grupo sem nome, busca o nome uma unica vez e cacheia em `Conversation.name` (sem chamar a API a cada mensagem). Adicionado o comando `sync_wapi_group_names` e o botao "Sincronizar grupos" na tela de Conversas (endpoint `conversas/sincronizar-grupos/`) para atualizar os nomes sob demanda. CSS `?v=7`.
- Corrigida a identificacao de GRUPO vs conversa DIRETA nas mensagens recebidas da W-API. Antes, mensagens de grupo caiam como conversa privada do participante (usava o remetente como se fosse o chat). Agora ha uma funcao central `normalize_wapi_message_context()` em `wapi/parser.py` que decide pelo ID da conversa: se termina em `@g.us` e GRUPO (chat_id = JID do grupo, remetente separado em sender_id/participant_id); numero puro / `@s.whatsapp.net` / `@lid` e DIRETA. O `Conversation` ganhou `chat_type` (`private`/`group`), `external_id` (JID/telefone/LID) e `name` (titulo do grupo), com `contact` agora opcional (grupo nao tem contato individual); o `Message` ganhou `sender_id`, `participant_id`, `is_group` e `from_me`. A ingestao foi centralizada em `wapi/services.py` (`ingest_wapi_payload` + `resolve_conversation_for_context`), usada pelo webhook e pelo comando `sync_wapi_events_to_conversations`, deduplicando pelo id externo e nunca criando contato privado para quem escreve no grupo. O envio de resposta passou a usar `conversation.recipient` (JID do grupo `@g.us` ou numero/LID da direta) via `normalize_recipient()` no `wapi/client.py`, nunca o participante. A tela Conversas ganhou abas Todas/Diretas/Grupos (filtro `tipo`), selo "Grupo" na lista e no cabecalho e o nome do participante acima das mensagens recebidas em grupo. Mensagens `from_me` de grupo/privado passam a aparecer como enviadas na conversa certa. Migrations `0012` (campos) e `0013` (backfill: conversas antigas como `private` com `external_id` do telefone; mensagens `out` antigas como `from_me=True`), sem apagar historico. CSS em `conversations.css` (`?v=6`).
- Corrigido o poll da tela Conversas que cortava o play de video/audio: a cada 6s o chat era totalmente reconstruido (`renderMessages` fazia `replaceChildren`), recriando o `<video>`/`<audio>` e interrompendo a reproducao mesmo sem mensagem nova. O poll passou a ser incremental (`syncMessages`): so anexa mensagem nova ou atualiza uma existente cujo conteudo mudou (ex.: midia que terminou de baixar), e NUNCA recria um elemento de midia que esteja tocando (`elIsPlaying`); sem mudancas, nao toca no DOM. O render completo (`renderMessages`/`buildMessageEl` extraido) segue sendo usado so ao abrir a conversa, com auto-scroll apenas quando o usuario ja estava no fim. A lista lateral ganhou assinatura (`listSignature`): o poll de 12s so reconstroi a lista quando algo realmente muda; trocas de filtro/aba/busca/sincronizar grupos forcam o re-render (`refreshList(true)`). Resultado: nada pisca a cada poucos segundos e o video nao para mais sozinho.
- Foto e video no chat passaram a abrir em tela grande (lightbox) e o video deixou de baixar inteiro na bolha (performance em grupos com muita midia). Na bolha, a foto vira uma miniatura clicavel e o video vira uma miniatura leve (primeiro quadro via `#t=0.1` + botao de play), carregada de forma preguicosa com `IntersectionObserver` (so quando entra em tela; antes cada video baixava metadados/arquivo ao renderizar). Clicar na foto/video (ou Enter/Espaco) abre um overlay grande centralizado: foto em alta ou `<video controls autoplay>`; fecha no X, no fundo ou com Esc, e ao fechar o `<video>` e removido para interromper play/download. Audio continua tocando direto na bolha e o GIF (mp4 curto) mantem o loop silencioso automatico. Figurinha segue inline. Apenas front-end: `conversations.html` (JS do lightbox/observer, split das ramificacoes video/gif) e `conversations.css` (miniatura de video, botao de play e lightbox); CSS `?v=8`.
- Abrir uma conversa passou a tentar rebaixar, em background, as midias que falharam na chegada (media_status 'unavailable'/sem arquivo local), sem loop automatico: o retry so dispara na ABERTURA (a chamada `mensagens/?retry=1`), nunca no poll de 6s. Novas funcoes em `wapi/services.py`: `_conversation_pending_media` (midias recebidas da conversa sem arquivo local e com payload), `retry_conversation_media_downloads` (sincrona, ate `limit=8` por vez) e `retry_conversation_media_async` (roda em `threading.Thread` daemon para nao travar a abertura quando um link estiver morto — o download tem timeout longo; usa um lock para nao rodar em paralelo na mesma conversa e so gasta thread quando ha algo pendente, fechando a conexao de banco ao fim). A view `conversation_messages_view` chama o retry apenas quando recebe `?retry=1`; o JS `openConversation` envia esse parametro so na abertura. A midia recuperada aparece sozinha no proximo ciclo do poll (o `syncMessages` ja troca a bolha quando `media_status`/`media_url` mudam), sem recarregar a tela. Reaproveita `retry_media_download` (download-media gera fileLink novo a cada chamada). Sem migration e sem mudanca de CSS.
- Corrigido o caso em que um grupo/canal do WhatsApp chegava como conversa DIRETA, criando um contato com "telefone" invalido (ex.: JID interno numerico "120363144038483540", 18 digitos). Causa: `normalize_phone` so rejeitava `@g.us`/`@lid` e nao tinha limite de tamanho, entao aceitava o id longo como telefone; e a classificacao so marcava grupo quando o chat_id terminava em `@g.us`. Agora em `wapi/parser.py`: `normalize_phone` tambem rejeita `@newsletter`/`@broadcast` e qualquer numero com mais de 15 digitos (E.164 max), e nova funcao `is_group_jid()` reconhece como conversa coletiva/nao-pessoal os sufixos `@g.us`/`@newsletter`/`@broadcast` e o id numerico "pelado" longo demais para telefone (JIDs de pessoa `@s.whatsapp.net`/`@c.us`/`@lid` seguem diretos). `normalize_wapi_message_context` passou a usar `is_group_jid` na deteccao/priorizacao do chat_id, entao canal/grupo sem `@g.us` nao vira mais contato falso. Adicionados 8 testes em `accounts/tests.py` (`WapiJidClassificationTests`) e o comando `cleanup_nonpersonal_conversations` (dry-run por padrao; `--delete` remove as conversas diretas invalidas ja existentes e os contatos-lixo orfaos). Sem migration.
- Mensagens de canal (@newsletter) e de transmissao/status (@broadcast) passaram a ser IGNORADAS na entrada: nova funcao `is_ignorable_jid()` em `wapi/parser.py` e um filtro em `ingest_wapi_payload` (ponto unico de recebimento) que descarta o payload sem criar conversa/contato, apenas registrando um log `[WAPI WEBHOOK] ignorado (canal/transmissao)`. Sao mensagens de mao unica, sem atendimento. Grupos (@g.us) e conversas diretas seguem normais. O comando `cleanup_nonpersonal_conversations` foi ampliado para tambem remover canais/transmissoes que porventura ja tenham virado conversa (privada ou grupo), alem das diretas com telefone invalido. Adicionados testes (`is_ignorable_jid` e `WapiIngestIgnoreTests`: newsletter/broadcast ignorados, grupo mantido). Suite: 28 testes OK. Sem migration.
- Corrigido o download de documentos que baixava como ".bin" em vez do arquivo real (ex.: .docx). Duas causas: (1) o arquivo era salvo localmente com extensao derivada de um mapa de mimetypes curto, que nao cobria docx/xlsx/pptx/etc. -> caia no default "bin"; (2) o link de download usava o nome do arquivo salvo no servidor (wapi_<id>.bin), nao o nome original. Backend (`wapi/services.py`): `_MIME_EXT` ganhou os tipos de Office/OpenDocument/PDF/texto/compactados, e nova funcao `_ext_for_media(message, mimetype)` escolhe a melhor extensao na ordem nome original do documento (fileName, guardado em message.text) -> mapa por mimetype -> `mimetypes.guess_extension` do Python -> "bin"; usada ao salvar o arquivo e no log de diagnostico. Front (`conversations.html`): o link do documento agora usa o atributo `download` com o nome original (ex.: contrato.docx), o que tambem corrige o nome ao baixar os arquivos ".bin" ja existentes no servidor (funciona por serem same-origin, em /media/). Imagem/audio/video continuam usando a extensao pelo mimetype (ja corretos), agora reforcada pela mesma funcao. Adicionados testes `WapiMediaExtensionTests`. Suite: 32 testes OK. Sem migration; sem mudanca de CSS.
- Fechada a brecha do download de documentos: qualquer tipo de arquivo passa a baixar com nome/extensao corretos, inclusive extensoes fora de qualquer lista (ex.: .dwg, .psd) e documentos enviados COM legenda. Antes, `caption` e `fileName` eram misturados em `message.text`, entao um documento com legenda perdia o nome real (voltava a virar .bin). Agora o parser expoe `filename` (fileName do WhatsApp) separado da legenda; nova funcao `document_filename(message)` em `wapi/services.py` le esse nome do `raw_payload` (com fallback para `message.text`), usada tanto ao salvar (`_ext_for_media`, que prioriza a extensao do nome original — nao depende do mapa de mimetypes) quanto na serializacao (`_serialize_message` envia `filename` ao front). O front (`conversations.html`) usa `msg.filename` no atributo `download` (nome real) e mantem a legenda como texto exibido. Como le do `raw_payload`, funciona tambem para documentos ja recebidos. Testes ampliados (`WapiMediaExtensionTests`: extensao arbitraria e documento com legenda). Suite: 34 testes OK. Sem migration; sem mudanca de CSS.
- Adicionadas notificacoes de nova mensagem na tela Conversas, estilo WhatsApp Web: pop-up do desktop (Web Notifications API, funciona com a aba em segundo plano em qualquer navegador, requer HTTPS e permissao) + aviso sonoro (beep sintetizado via Web Audio API, sem arquivo). Um poll dedicado a cada 6s consulta todas as conversas (`lista/?status=todas&tipo=todas`) e detecta aumento do `unread_count` por conversa (o 1o poll so semeia o estado, nao notifica; nunca notifica mensagem enviada, pois `unread` so cresce em recebidas). Quando a aba esta em segundo plano mostra o pop-up nativo; quando esta em foco em outra conversa mostra um toast; nao notifica a conversa que ja esta aberta e em foco. Clicar no pop-up foca a janela e abre a conversa. O titulo da aba passa a mostrar o total de nao lidas (ex.: "(3) Conversas | BEEZap"). Novo botao "Som" no topo da lista liga/desliga o aviso sonoro (persistido em localStorage `beezap_sound`; ao ligar, toca um beep de confirmacao, destrava o audio e aproveita o gesto para pedir a permissao do pop-up). O audio e destravado no 1o clique/tecla (exigencia dos navegadores). URL do logo passada via `data-logo` no `conv-body` (mantem o JS sem tags Django). CSS `?v=9` (`.conv-head-actions`, `.conv-sound-toggle`). Sem migration; sem mudanca de backend.
- Adicionado botao dedicado "Notificacoes" ao lado de "Som", que reflete e controla a permissao do navegador. Motivo: o pedido de permissao no carregamento era ignorado pelos navegadores (so aceitam a partir de um clique do usuario), entao a permissao nunca era solicitada. Agora o botao detecta o estado (`Notification.permission`) e muda de cor: verde "Notificacoes ativas" (granted), ambar "Ativar notificacoes" (default, precisa clicar) e vermelho "Notificacoes bloqueadas" (denied, orienta a liberar no cadeado). Clicar quando default forca `Notification.requestPermission()` (gesto valido) e mostra uma notificacao de teste ao permitir; quando denied, explica como desbloquear. A cor se atualiza sozinha via `navigator.permissions.query({name:'notifications'}).onchange`. Removidas as chamadas antigas de `ensureNotifyPermission()` (load e toggle de som) que nao funcionavam. CSS `?v=10` (`.conv-notif-toggle` com estados on/off/blocked). Sem backend novo.
- Ajustados os botoes de Som e Notificacoes na tela Conversas: viraram compactos so-icone (base `.conv-icon-btn`, 32px redondo) porque com texto os tres botoes (Som, Notificacoes, Sincronizar grupos) estouravam a coluna estreita da lista e o rotulo longo "Notificacoes bloqueadas" ficava enorme/feio. O estado da permissao agora e indicado por um anel colorido (verde=ativa, ambar=ativar, vermelho=bloqueada) e o texto foi para o tooltip; o som usa opacidade quando desligado. Mensagem do estado "bloqueada" ficou mais curta. Observacao tecnica: quando o usuario ja negou a permissao, o navegador NAO permite pedir de novo via codigo (trava de seguranca) — so da para reativar nas configuracoes do site (cadeado); por isso o clique nesse estado apenas orienta. CSS `?v=11`.
- Unificado num unico botao "Atualizar" (icone de refresh, ao lado de Som/Notificacoes) as acoes de sincronizar nomes de grupos e retentar midias que falharam. Removido o botao de texto "Sincronizar grupos" (e seu CSS). Ao clicar, o botao: (1) chama `sincronizar-grupos/` para puxar os nomes reais dos grupos pela W-API; (2) se ha conversa aberta, recarrega as mensagens com `?retry=1`, disparando em background a retentativa das midias sem arquivo local; (3) atualiza a lista. O icone gira (`.spinning`) enquanto processa. Removido o disparo automatico de retry na ABERTURA da conversa (`openConversation` nao envia mais `?retry=1`) — agora e sob demanda por este botao; o backend segue igual (a view so retenta quando recebe `?retry=1`). CSS `?v=12` (`.conv-refresh-btn`, animacao `conv-spin`, `.conv-icon-btn:disabled`). Sem migration; sem mudanca de backend.
- Corrigido: atualizacoes de STATUS do WhatsApp ('stories', JID status@broadcast) entravam como conversa direta do autor (ex.: "Marcia Nunes" recebendo posts de status aleatorios, gerando notificacao sem conversa real no celular). O filtro anterior so olhava o chat_id; o W-API Lite manda o autor como remetente e o status@broadcast em outro campo, entao escapava. Nova funcao `is_status_or_broadcast(payload)` em `wapi/parser.py` detecta status por flag de broadcast (`broadcast`/`isStatus` em varios caminhos) ou pela presenca de "status@broadcast" em QUALQUER lugar do payload (busca recursiva com limite de profundidade). `ingest_wapi_payload` passou a ignorar quando `is_ignorable_jid(chat_id)` OU `is_status_or_broadcast(payload)`. Novo comando `cleanup_status_messages` (dry-run; `--delete`) remove as mensagens de status ja recebidas (mantendo mensagens reais do contato) e apaga conversas que ficarem vazias, atualizando o resumo das afetadas. Testes: `WapiStatusDetectionTests` e casos em `WapiIngestIgnoreTests` (status@broadcast direto e com autor como remetente). Suite: 39 testes OK. Sem migration; sem mudanca de CSS.
- Verificacao das notificacoes de grupo x direta: a deteccao NAO diferencia tipo (e feita pelo aumento de unread_count, que vale igual para grupo e direta; o backend `lista/?tipo=todas` retorna ambos com unread). O que decidia pop-up vs. toast interno era apenas `document.hidden` (aba totalmente oculta). Como grupos costumam chegar com a aba visivel, viravam toast; diretas percebidas em outro app viravam pop-up — dando a impressao de que grupo nao gerava pop-up. Ajustado para usar tambem `document.hasFocus()`: o pop-up do desktop passa a aparecer sempre que a janela do BEEZAP NAO estiver em foco (mesmo com a aba visivel num monitor ao lado), valendo igualmente para grupo e direta; com a janela em foco, mostra o toast (exceto na conversa ja aberta). Sem mudanca de backend nem CSS.
- Dois ajustes em conversas de grupo. (1) "Tipo de mensagem nao suportado" fantasma: mensagens de tipo `unknown` (quase sempre de SISTEMA do WhatsApp, como `senderKeyDistributionMessage`/`protocolMessage` que aparecem toda hora em grupos) eram criadas e exibidas. Agora `ingest_wapi_payload` ignora `message_type == 'unknown'` (com log de diagnostico das chaves de conteudo via `wapi_content_keys`); nada e criado. Novo comando `cleanup_unknown_messages` (dry-run; `--delete`) remove as ja existentes e apaga conversas que ficarem vazias. (2) Nome do remetente aparecendo como "." (ou so pontuacao): `_valid_name` passou a exigir ao menos um caractere alfanumerico, entao nomes so de simbolo sao rejeitados; no front, quando nao ha nome valido, a mensagem de grupo recebida mostra o NUMERO do participante (`sender_id`) em vez de ficar em branco/". Testes: `WapiUnknownMessageTests`, `WapiSenderNameTests`. Suite: 43 testes OK. Sem migration; sem CSS.
- Adicionado comando de diagnostico `inspect_wapi_messages` para investigar de onde vem mensagens que caem no lugar errado (ex.: status/transmissao entrando como conversa direta). Filtra por `--name` (nome/telefone de contato, grupo ou remetente), `--conv <id>` e `--limit`; para cada mensagem mostra os dados salvos, o que o parser ATUAL decide (chat_id, grupo/direta, is_status_or_broadcast, is_ignorable_jid, tipo detectado), as chaves de conteudo e do payload; com `--full` imprime o payload cru (valores longos truncados). Read-only; o payload e do webhook recebido e nao contem o token da W-API. Sem migration; sem CSS.
- Corrigido de vez o Status entrando como conversa: o diagnostico (comando inspect_wapi_messages) revelou que o W-API Lite manda atualizacoes de Status ('stories') com `chat.id == "status"` (NAO `status@broadcast`) e o autor no `sender` — por isso viravam a conversa direta #8 `external_id='status'` juntando o status de varios contatos. `is_status_or_broadcast()` passou a detectar: `chat.id`/`remoteJid` igual a "status" (ou terminando em @broadcast), flags de broadcast, e os marcadores exclusivos de status `statusSourceType`/`posterStatusID` (busca por chave em qualquer nivel). `is_ignorable_jid()` tambem trata o id literal "status". Assim o ingest ignora esses eventos e o comando `cleanup_nonpersonal_conversations` remove a conversa "status" ja existente. Filtro do `cleanup_status_messages` ampliado (status nem sempre tem a palavra "broadcast"). Testes com o payload real capturado. Suite: 46 testes OK. Sem migration; sem CSS.
- Mencoes em grupo (@) passam a mostrar o nome em vez do numero cru. No WhatsApp, mencionar alguem coloca "@<numero/LID>" no texto e o app resolve para o nome; no sistema aparecia "@140437377568773". Agora, ao abrir uma conversa de grupo, o backend monta um mapa {digitos: nome} a partir dos participantes que ja enviaram mensagem no grupo (sender_id -> sender_name) e substitui "@<numero>" por "@<nome>" no texto/legenda (funcao `_resolve_mentions` + `_build_mention_map` em `accounts/views.py`, aplicadas em `_serialize_message`). Mencoes de quem ainda nao enviou nada no grupo (nome desconhecido) permanecem como numero. So leitura/exibicao; nao altera o texto salvo. Testes: `MentionResolutionTests`. Suite: 50 testes OK. Sem migration; sem CSS.
- Nomear participante de grupo pelo numero: quando o remetente de uma mensagem de grupo nao tem nome (nao esta nos contatos e sem pushName valido), o NUMERO aparece clicavel; ao clicar abre um modal "Nomear contato" para digitar o nome e salvar. Novo endpoint `conversas/nomear-contato/` (`conversation_name_contact_view`) cria/atualiza um `Contact` (phone=digitos, name). A resolucao de nomes passou a considerar tambem esses contatos: `_build_mention_map` virou `_build_name_map`, que junta pushName (dos remetentes) + Contatos salvos (Contato tem PRIORIDADE) e cobre tanto o remetente exibido quanto as mencoes `@numero` no texto. `_serialize_message` passou a mandar o `sender_name` resolvido (Contato > pushName; vazio => front mostra o numero clicavel). Assim, ao nomear alguem, o nome aparece no remetente e nas mencoes a ele, em todas as mensagens (o poll incremental troca as bolhas pois `sender_name`/`text` mudam). Front: bolha do remetente vira botao quando so ha numero; modal + POST + refresh. CSS `?v=13` (`.conv-msg-sender-num`, `.conv-name-modal`). Testes: `ContactNamingTests`. Suite: 53 testes OK. Sem migration (reusa Contact).
- Implementada a tela de Contatos (item "Contatos" da barra lateral, antes sem link). Nova view `contacts_view` + rota `contatos/` + template `accounts/contacts.html` + `static/css/contacts.css`. Lista os contatos (avatar com iniciais, nome, telefone), com busca por nome/telefone (GET `q`), contador, e CRUD: adicionar/editar via modal e excluir (com confirmacao). O telefone e normalizado para digitos ao salvar (ex.: "+55 (16) 99999-8888" -> "5516999998888"), a mesma chave usada na resolucao de nomes das mensagens de grupo — entao contatos cadastrados aqui aparecem automaticamente no lugar do numero (remetente e mencoes @). Reaproveita `dashboard.css`/`attendants.css` (shell, topbar, botoes) e usa mensagens/toast do base.html. Disponivel para qualquer usuario logado. Testes: `ContactsPageTests`. Suite: 59 testes OK. Sem migration (reusa o modelo Contact).
- Em conversa DIRETA, o nome do contato no cabecalho do chat virou clicavel (sublinhado pontilhado no hover): clicar abre o modal "Nomear contato" ja preenchido com o nome atual, para renomear. Salva no mesmo Contact (endpoint `nomear-contato/`), atualiza o cabecalho na hora e reflete na lista de conversas e na tela de Contatos. `fillInfo` passou a guardar o contexto da conversa aberta (`currentIsGroup`, `currentContactPhone`, `currentContactName`) e a marcar o nome como editavel so em conversa direta; `openNameModal` aceita nome inicial; ao salvar, se o numero for o do contato aberto, o cabecalho e atualizado. (As conversas diretas ja eram salvas automaticamente em Contatos na 1a mensagem, via get_or_create_contact — comportamento inalterado.) Somente frontend; CSS `?v=14` (`.conv-chat-name-editable`). Sem migration.
- Documentacao atualizada para refletir o estado atual apos as features desta fase: `docs/CONTEXTO.md` (Contact e base de contatos/resolucao de nomes; parser com is_group_jid/is_ignorable_jid/is_status_or_broadcast/normalize_phone com limite de 15 digitos; services ignorando status/canal/sistema e _ext_for_media/document_filename/retry async; secao 5 de Conversas com poll incremental, lightbox, mencoes, nomear remetente/cabecalho, notificacoes pop-up+som+botoes e botao Atualizar; nova secao 5.1 Tela Contatos; cache-busting em v=14; lista de comandos de management; pendencias revisadas). `docs/CODEX_PADROES.md` e `docs/INDEX.md` ajustados. Nova regra de trabalho: a documentacao (CONTEXTO.md e afins) deve ser atualizada no MESMO commit de cada modificacao, refletindo sempre o estado atual.
- Correcao no envio de midia pela conversa: a W-API roda na nuvem e so baixa a midia se a URL publica for acessivel de fora. Em ambiente local (localhost/IP privado) o envio de imagem/audio/video/documento falhava com "Nao foi possivel enviar... verifique a conexao do WhatsApp". Agora `conversation_send_media_view` detecta o host (`_host_reachable_by_wapi`): host publico continua usando a URL (producao inalterada); localhost/IP privado/.local passa a enviar a midia em base64 (data URI via `_media_file_to_data_uri`), aceito pela W-API. Envio local de imagem volta a funcionar.
- Correcao no envio de TEXTO pela conversa: o campo do composer era `<input type="text">`, que NAO aceita quebras de linha — ao colar um texto estruturado (varias linhas/paragrafos) tudo chegava no WhatsApp como um paragrafo unico, e os simbolos de Markdown (`**`, `#`, `>`) iam literais. Agora: (1) o campo virou `<textarea>` que cresce sozinho (Enter envia, Shift+Enter quebra linha, igual WhatsApp Web) preservando as quebras; (2) novo modulo `wapi/formatting.py` com `markdown_to_whatsapp()` converte no envio a formatacao Markdown para a nativa do WhatsApp (`**negrito**`->`*negrito*`, titulos `#` viram linha em negrito, listas `*/-/+`->`•`, links `[t](url)`->`t (url)`, citacao `>` e lista numerada mantidas). Aplicado em `conversation_send_view`; o historico guarda a mesma versao enviada. Frontend so (CSS `?v=15`, textarea auto-grow) + backend. Sem migration.
- Correcao no envio de IMAGEM (VPS): o log mostrou o motivo real — a W-API responde HTTP 500 `{"error":true,"message":"A URL da imagem deve ser nos formatos .png, .jpeg ou .jpg."}`. O `MEDIA_URL` estava correto; o problema era a EXTENSAO da URL: imagens salvas com extensao fora de {.png,.jpg,.jpeg} (ex.: `.webp`, `.jfif`, print colado) faziam a W-API recusar. Agora, antes de salvar, `ensure_wapi_image()` (em `wapi/services.py`, chamada por `conversation_send_media_view`) garante o formato: PNG/JPEG so tem a extensao normalizada (`image.png`/`image.jpg`); outros formatos (webp/gif/bmp/heic/...) sao convertidos para JPEG via `_convert_image_to_jpeg()` (ffmpeg, ja usado para audio). Assim a URL sempre termina em `.jpg`/`.png`. Se o ffmpeg faltar e o formato nao for png/jpeg, retorna erro amigavel. Sem migration.
- Dependencias explicitadas para o sistema nao quebrar por falta de dependencia de S.O.: `requirements.txt` reescrito com cabecalho claro (Python 3.12+, pacotes pip e bloco de dependencias de SISTEMA — ffmpeg OBRIGATORIO para midia, nginx, git, com comandos de instalacao por S.O.). Novo system check `accounts/checks.py` (registrado em `AccountsConfig.ready`) que emite WARNING `beezap.W001` no `manage.py check` quando o ffmpeg nao esta no PATH — assim a falta aparece no deploy, nao so em producao. Docs atualizados: `DEPLOY.md` (secao Dependencias do sistema com ffmpeg p/ audio+imagem e verificacao), `DEPLOY_VPS.md` (ffmpeg adicionado ao `apt install` do pre-requisito e ao checklist), `CONTEXTO.md` (nota de ffmpeg). Sem migration.
- Correcao no envio de DOCUMENTO (VPS): o log mostrou HTTP 500 `{"error":true,"message":"A extensão do arquivo é obrigatória."}`. O `send-document` da W-API exige o campo `extension` no corpo, que nao era enviado. Agora `send_document_message()` (wapi/client.py) inclui `extension`; `conversation_send_media_view` calcula a extensao a partir do nome do arquivo (ex.: `Contrato.pdf`->`pdf`), com fallback por mimetype (`WAPI_DOC_EXT_BY_MIME`: pdf/doc/docx/xls/xlsx/ppt/pptx/txt/csv). Testado e2e: payload send-document sai com `fileName` + `extension`. Sem migration.
- Documento ENVIADO passa a mostrar o nome real no chat (antes vinha "Baixar documento" generico). O arquivo em disco usa nome uuid, entao `save_outgoing_media_message` guarda o nome ORIGINAL em `raw_payload={'beezap_filename': ...}` e `document_filename()` passou a le-lo primeiro (documento recebido continua lendo do payload do webhook). O serializer/JS ja usavam o campo `filename`, entao nada mudou neles; o chat exibe "📄 <nome>.pdf" e baixa com o nome/extensao corretos. 59 testes OK. Sem migration.
- Correcao de reenvio duplicado no composer: ao apertar Enter varias vezes seguidas para enviar uma mensagem de texto, a mesma mensagem era enviada uma vez por Enter. Causa: o `<textarea>` so era limpo DEPOIS que o `fetch` assincrono retornava, entao cada Enter durante a requisicao chamava `requestSubmit()` de novo com o texto ainda presente (o `sendBtn.disabled` nao bloqueava, pois o Enter chama requestSubmit direto). Agora ha uma trava `sendingMessage`: o submit ignora novas chamadas enquanto uma esta em andamento e libera no `finally`. Somente frontend (JS inline em `conversations.html`); sem CSS, sem migration.
- Feedback visual de "enviando" no composer: ao apertar Enter/clicar em enviar, o botao verde do aviao vira um circulo cinza com spinner girando enquanto a mensagem esta sendo enviada, voltando ao normal quando termina (sucesso ou erro). Implementado com um 2o SVG (`.conv-send-spin`) dentro do botao e a classe `.is-sending` (alternada junto com a trava `sendingMessage`); animacao CSS `conv-send-spin`. CSS `?v=16`. Somente frontend; sem migration.
- Atendente virtual (IA) — Ollama leve no VPS: para nao sobrecarregar o servidor pequeno (4 GB compartilhado), o modelo passa a DESCARREGAR da RAM quando ocioso. Novo `keep_alive` no `chat_with_ollama` (`ai_engine/client.py`), setting `OLLAMA_KEEP_ALIVE` (padrao `30s`, tambem no `.env.example`), passado pelo `classify_intent`. Documentada em `docs/DEPLOY.md` a instalacao/config leve do Ollama no VPS (install, `ollama pull qwen2.5:1.5b`, override do systemd com `OLLAMA_KEEP_ALIVE`/`OLLAMA_MAX_LOADED_MODELS`/`OLLAMA_NUM_PARALLEL`, verificacao com `ollama ps`/`free -h`). Sem migration.
- Atendente virtual (IA) — TELA de administracao (ADM). Nova tela "Atendente Virtual" (item na barra lateral + 3a aba na area de Automacao) para ligar/desligar o bot e configurar: nome da empresa, mensagem de boas-vindas (usa `{empresa}`), setor padrao (fallback) e tentativas antes de transferir. Nova view `ai_attendant_settings_view` (`accounts/views.py`), form `AiAttendantConfigForm` (`accounts/forms.py`, ModelForm de `AiAttendantConfig`), rota `automacao/atendente-virtual/` (`ai-attendant-settings`), template `accounts/ai_attendant_settings.html` (reusa `dashboard.css`/`automation_ai.css`). Avisa para cadastrar setores se nao houver. Testes `AiAttendantSettingsViewTests` (ADM abre/salva; nao-ADM recebe 403). Suite: 75 testes OK.
- Atendente virtual (IA) — BACKEND (motor + ligacao ao fluxo real). Interruptor vem DESLIGADO por padrao (`AiAttendantConfig.enabled=False`), entao nada muda ate o ADM ligar; a tela de config vem no proximo commit. Objetivo: a IA recepciona o cliente numa conversa direta, entende a intencao e transfere para o setor certo, deixando a conversa "Aguardando". Criados: model singleton `AiAttendantConfig` (`enabled`, `company_name`, `welcome_message` com `{empresa}`, `fallback_sector`, `max_turns`; `get_solo()` no padrao de `WapiConfiguration`); campos `Conversation.ai_state` (`active`/`handed_off`/`off`) e `Conversation.ai_turns`; campo `Message.is_ai` (marca falas do bot, permite detectar quando um humano assume). Motor em `ai_engine/`: `classify_intent()` (`services.py`) decide o setor em 2 camadas — 1) palavras-chave das regras de atendimento com setor (deterministico, prioridade), 2) IA local classificando em 1 setor da lista ou `INDEFINIDO` (novo prompt `build_intent_classification_messages` em `prompts.py`); e `ai_engine/attendant.py` com a maquina de estados `handle_incoming_for_ai` (boas-vindas no 1o contato; roteia com intencao clara marcando `sector`+`status=pending`+`ai_state=handed_off`; pede para esclarecer ate `max_turns` e entao transfere para o fallback; para quando um humano assume; ignora grupo/fechada/atribuida/desligada) + falas em TEMPLATE fixo (nao geradas pela IA) + disparo em background `handle_incoming_for_ai_async` (thread daemon, lock por conversa, guardas baratas, no padrao de `retry_conversation_media_async`). LIGACAO: `save_incoming_message` (`wapi/services.py`) aciona o atendente virtual em background apos salvar toda mensagem RECEBIDA (nunca bloqueia o webhook). Migration `0014`. `is_ai` adicionado a `save_outgoing_message`/`save_outgoing_text_message`. Motor plugavel (trocar para Claude depois so mexe no seam do `ai_engine`, sem refazer). Testes: `AiIntentClassificationTests` e `AiAttendantFlowTests` (com `chat_with_ollama` mockado; inclui guardas do disparo e o acionamento pelo ingest). Suite: 73 testes OK.
- CORRIGIDO o bug de "mandaram varias fotos de uma vez e so as figurinhas chegaram" (e o mesmo bug fazia GIFs/videos sumirem): fotos/videos/gifs NORMAIS estavam sendo DESCARTADOS como se fossem Status/stories. Causa confirmada por payload real capturado na VPS: o WhatsApp hoje coloca `contextInfo.statusSourceType` (`"IMAGE"`/`"VIDEO"`/`"GIF"`) em midia comum, so para indicar que ela PODE ser repostada como status; a funcao `is_status_or_broadcast` (`wapi/parser.py`) usava `statusSourceType` como marcador de Status e por isso jogava fora um lote inteiro de fotos (ex.: 18 fotos numa conversa direta, todas descartadas com `status_or_broadcast=True`). Fix: `_STATUS_MARKER_KEYS` deixou de conter `statusSourceType` e ficou so com `posterStatusID` (id real do post de status); Status de verdade continua pego por `chat.id == "status"`, `@broadcast`/`status@broadcast` e `posterStatusID`. Assim as fotos/videos/gifs recebidos em conversa e grupo voltam a aparecer. O album (`albumMessage`) segue como cabecalho ignorado — as fotos do album chegam como `imageMessage` individuais e agora entram normalmente. Testes de regressao com os payloads reais: `test_normal_media_with_status_source_type_is_not_status` e `test_real_status_still_detected_without_source_type`. Suite: 61 testes OK. Sem migration; sem CSS. (Para recuperar as midias ja descartadas, rodar `python manage.py sync_wapi_events_to_conversations`.)
- Diagnostico do caso "mandaram varias fotos de uma vez e so as figurinhas chegaram": criado o comando `inspect_wapi_events` que inspeciona os EVENTOS BRUTOS do webhook (tabela `WapiWebhookEvent`), diferente do `inspect_wapi_messages` (que so mostra `Message` ja criadas). Para cada webhook recebido — inclusive os que o `ingest_wapi_payload` DESCARTOU — mostra o veredito do parser (grupo/direta, tipo detectado, status/broadcast), as chaves do conteudo e se o ingest CRIARIA ou DESCARTARIA a mensagem e por que (sem chat_id / status-canal-transmissao / tipo nao suportado / texto vazio / duplicada), alem de indicar se existe uma `Message` com o mesmo id externo. Filtros `--name`, `--contains <texto no payload, ex.: albumMessage>`, `--hours N`, `--limit`, `--full`. Read-only; o payload nao contem o token da W-API. Hipotese sob investigacao: album de fotos (varias de uma vez) chega como `albumMessage`, que o parser ainda nao reconhece e cai como `unknown` -> ingest ignora; figurinhas chegam uma a uma como `stickerMessage` normal. Falta confirmar a estrutura real do payload na VPS para escrever o parser do `albumMessage`. Sem migration; sem CSS.
- Transferencia manual de atendimento ajustada para refletir o fluxo real no painel admin: ao definir um setor sem atendente, a conversa passa para `status='pending'` (Aguardando/Pendente); ao definir um atendente, volta para `status='open'` (em atendimento). Qualquer transferencia manual desliga a IA da conversa (`ai_state='off'`) para impedir que o atendente virtual continue respondendo depois da intervencao humana/admin. O endpoint `conversation-transfer` segue devolvendo os dados atualizados para a coluna lateral da tela Conversas, incluindo setor, atendente e status. Testes adicionados em `ConversationTransferViewTests`. Sem migration; sem CSS.
- Atendente virtual/recepcao ajustado para nao reabrir IA em conversa ja roteada: se a conversa ja possui setor ou atendente, o bot nao envia boas-vindas nem tenta classificar de novo. A lista de Conversas agora recebe `queue_label` e mostra uma etiqueta discreta como "Aguardando Compras" quando uma conversa esta `pending` em um setor sem atendente, deixando explicito o novo atendimento na fila do setor. CSS `conversations.css?v=17`. Testes atualizados em `AiAttendantFlowTests` e `ConversationTransferViewTests`. Sem migration.
- VPS/IA: instalado Ollama no servidor em modo CPU-only, configurado override systemd leve (`OLLAMA_KEEP_ALIVE=30s`, `OLLAMA_MAX_LOADED_MODELS=1`, `OLLAMA_NUM_PARALLEL=1`) e baixado o modelo `qwen2.5:1.5b`. Adicionado o comando `seed_ai_sector_rules` para criar/atualizar regras basicas de roteamento para Compras/Vendas e Financeiro, mantendo a camada deterministica por palavras-chave antes do modelo local. Documentados comandos em `requirements.txt`, `docs/DEPLOY.md` e `docs/CONTEXTO.md`.
- IA/regras: o comando `seed_ai_sector_rules` tambem passa a preencher descricoes vazias dos setores Compras/Vendas e Financeiro, e amplia palavras-chave de Compras para proposta, comercial, servico e fornecimento. Isso melhora tanto a camada deterministica quanto o prompt enviado ao Ollama.
- VPS/IA: Atendente Virtual ativado no banco de producao (`AiAttendantConfig.enabled=True`, empresa BEEZAP, `max_turns=3`, fallback sem setor para nao rotearmos indefinidos para Compras/Financeiro indevidamente). Validado fluxo com conversa temporaria e envio mockado: primeira mensagem gerou boas-vindas; mensagem de boleto roteou para Financeiro com `status=pending` e `ai_state=handed_off`.
- IA/recepcao: corrigida a guarda que bloqueava conversas antigas com mensagens humanas no historico. Agora mensagem humana antiga nao impede nova recepcao; humano so desliga a IA quando responde depois de uma fala da propria IA. Conversas antigas sem setor/atendente, `ai_state=off` e `ai_turns=0` sao reativadas automaticamente ao receber nova mensagem.
- Ciclo de atendimento: adicionadas acoes reais de `Assumir` e `Encerrar` na tela Conversas. Encerrar marca a conversa como `closed` e remove atendente; como o ingest ja ignora conversas fechadas ao resolver novas mensagens, o proximo contato do mesmo numero cria nova conversa aberta, sem setor/atendente, voltando para a recepcao da IA. A lista agora re-renderiza quando status/fila mudam. Testes cobrem assumir, encerrar e novo ciclo apos conversa fechada. CSS `conversations.css?v=18`.
- Documentacao: `docs/DEPLOY_VPS.md` passou a registrar o estado real do VPS em 2026-07-08 (app em `/var/www/beezap`, servicos `beezap`/`ollama`, porta 8103, prefixo `/beezap`, SQLite atual, Ollama `qwen2.5:1.5b`, override leve, Atendente Virtual ativo, regras IA aplicadas e ciclo de atendimento). `docs/CONTEXTO.md` removido da pendencia antiga de assumir/encerrar, pois a funcionalidade foi concluida.
- IA/roteamento: adicionada trava deterministica para mensagens vagas/ambiguas sem palavra-chave de setor. Frases como "na tenho certeza ta dando tudo errado" agora retornam `INDEFINIDO` e fazem a IA pedir esclarecimento, em vez de deixar o Ollama chutar Financeiro/Compras. Teste de regressao em `AiIntentClassificationTests`.
- IA/recepcao: corrigido limbo de conversa antiga com `ai_state=off`, `ai_turns>0`, sem setor e sem atendente. Agora a IA reativa na proxima mensagem quando nao houve resposta humana depois da fala da propria IA; isso evita que mensagens como "bom dia" entrem no webhook e fiquem sem resposta por estado residual. Teste de regressao em `AiAttendantFlowTests`.
- IA/recepcao: a classificacao antes da transferencia passou a usar uma janela curta das ultimas mensagens do cliente, evitando analisar cada frase isolada. Cumprimentos soltos apos pedido de esclarecimento recebem uma fala contextual diferente, em vez de repetir o mesmo "Desculpe, ainda nao entendi...". Testes cobrem contexto recente e cumprimento depois de esclarecimento.
- IA/recepcao: adicionado modo de teste "IA decide sozinha" (`AiAttendantConfig.llm_only`, checkbox no painel Atendente Virtual). Quando ligado, `classify_intent(..., llm_only=True)` pula a camada deterministica (palavras-chave das regras + trava anti-ambiguidade) e deixa o modelo local decidir o setor por conta propria (o laco de esclarecer/`INDEFINIDO`/fallback continua). Alem disso, a classificacao passou a receber `history`: um resumo curto de conversas ANTERIORES com o mesmo contato (montado por `_contact_history_context`/`_sibling_conversation_ids` no orquestrador, por contato ou pelo external_id da direta), incluido no prompt (`build_intent_classification_messages(..., history=)`) para a IA se inteirar do contexto — vale nos dois modos. Migration `0015` (campo `llm_only`). Testes: modo llm_only pula palavra-chave/ambiguidade, history chega ao prompt, e historico cross-conversa do contato. Suite: 90 testes OK. Painel/CSS reaproveitados; sem mudanca de CSS.
- IA/recepcao: adicionado campo `AiAttendantConfig.instructions` (prompt/diretrizes editaveis no painel Atendente Virtual, no estilo "voce e o atendente da empresa X, escolha o setor certo, o que fazer / o que nao fazer"). Essas instrucoes viram o SYSTEM PROMPT da classificacao (`build_intent_classification_messages(..., instructions=)`), com um default pronto e substituicao de `{empresa}` (`render_instructions()`). O codigo sempre anexa uma regra de formato fixa (`_OUTPUT_FORMAT_RULE`) que obriga a saida a ser so o nome do setor ou INDEFINIDO, entao instrucoes livres nao quebram o parsing/roteamento. As falas enviadas ao cliente continuam vindo dos templates (boas-vindas + avisos de transferencia). `classify_intent` e o orquestrador passam `instructions=config.render_instructions()`. Migration `0016`. Testes: instrucoes custom chegam ao system prompt junto da regra de formato; tela salva/renderiza o campo. Suite: 91 testes OK.
- IA/recepcao: adicionado MODO GENERATIVO (`AiAttendantConfig.generative_replies`, checkbox no painel). Quando ligado, a IA GERA as respostas ao cliente a partir das `instructions` (nao usa mais os templates de boas-vindas/esclarecer/transferir) e decide o roteamento por um marcador `[SETOR: X | GERAL | CONTINUAR]` que o cliente nao ve. Novo `build_generative_reply_messages` (prompt: persona + setores injetados + regra de marcador) e `generate_reply_and_route` em `ai_engine/services.py`, com parsing tolerante (`_extract_marker`/`_resolve_marker`): setor nomeado -> encaminha; GERAL -> setor "Geral" ou fallback; CONTINUAR/sem marcador -> segue a conversa (seguro). No orquestrador, `_handle_generative_turn` envia a fala da IA e roteia com `_route_to_sector(..., announce=False)` (a IA ja avisou); `_conversation_transcript` monta a conversa atual; ao atingir `max_turns` sem decisao, encaminha ao fallback. Fala minima de seguranca (`GENERATIVE_SAFETY_REPLY`) so quando o modelo local esta fora do ar, para nao deixar o cliente sem resposta. Default de `instructions` reescrito no estilo pedido (persona + regras do que nao fazer + Setor Geral como fallback; sem lista fixa de setores, que sao injetados). Migration `0017`. Testes: `AiGenerativeReplyTests` (parsing/roteamento/indisponivel) + fluxo generativo no orquestrador (encaminhar e continuar). Suite: 98 testes OK. Aviso: com modelo pequeno o modo generativo e mais instavel; para producao com qualidade, o seam para Claude Haiku ja existe.
- IA/contexto: ampliado o historico do contato usado como contexto da IA para as ULTIMAS ~10 TROCAS (≈20 mensagens, constante `CONTACT_HISTORY_MAX_MESSAGES` em `ai_engine/attendant.py`), varrendo ate 10 conversas anteriores do mesmo contato (`_sibling_conversation_ids` limit=10). Vale nos dois modos (classificacao e generativo). Antes eram so 8 mensagens. Sem migration.
- IA/generativo: corrigido o caso em que a conversa "parava de responder sem transferir". No modo generativo, ao atingir `max_turns` sem o modelo decidir o setor, o handoff era feito para o fallback com `announce=False` (e no VPS o fallback e "sem setor"), entao a conversa ia para uma fila silenciosa e as mensagens seguintes nao tinham resposta. Agora o encaminhamento no limite e feito AVISANDO o cliente (`announce=True`), e o mesmo vale quando a IA decide o setor mas nao escreve nada — nunca fica mudo. Tambem: novo `OLLAMA_GENERATIVE_TIMEOUT` (padrao 60s) usado pelo `generate_reply_and_route`, para o modo generativo nao cair na fala de seguranca ("So um momento...") por timeout/partida a frio na 1a mensagem (gerar texto + carregar o modelo no CPU demora mais que classificar). Sem migration. Testes: `test_generative_mode_gives_up_with_notice` e `test_generative_route_without_reply_still_announces`. Suite: 100 testes OK.
- IA/generativo: corrigido o caso em que a IA nao mandava NADA ao cliente. No ramo "continue", quando o modelo respondia mas nao escrevia texto ao cliente (ex.: so o marcador `[SETOR: ...]`, comum em modelo pequeno) e o reply ficava vazio com `available=True`, o bot ficava mudo. Agora nunca fica em silencio: fala vazia com modelo disponivel -> `GENERATIVE_EMPTY_FALLBACK` (pedido curto de esclarecimento); modelo fora do ar -> `GENERATIVE_SAFETY_REPLY`. Adicionados logs de diagnostico no logger `beezap.ai`: no modo generativo loga action/setor/available/reply/raw do modelo; e as guardas de `handle_incoming_for_ai` passam a logar por que a IA NAO atua (ja tem atendente / ja roteada para setor / ai_state != active) — util para diagnosticar "por que nao respondeu" (ex.: conversa antiga ja transferida). Teste `test_generative_continue_empty_reply_never_silent`. Suite: 101 testes OK. Sem migration.
- IA/generativo (performance): o modo generativo com 3B no VPS (CPU, 4 GB) estourava o timeout SEMPRE (log `available=False reply='' raw=''`) porque o prompt ficou gigante (historico de ~20 mensagens) e a resposta longa (num_predict 240) — no CPU, gerar token a token e o gargalo. Ajustes para caber no tempo: `num_predict` do generativo cai para 120 (novo `OLLAMA_GENERATIVE_NUM_PREDICT`, resposta de recepcao e curta); historico e transcricao enviados no generativo enxutos para 6 mensagens; `OLLAMA_GENERATIVE_TIMEOUT` subiu para 90s de margem. Sem migration. Observacao registrada: local (1.5b/3b) em CPU 4 GB nao entrega generativo rapido; para conversa livre fluida, 1.5b (mais rapido/limitado), VPS maior, ou Claude Haiku (rapido, o seam ja existe).
- IA/diagnostico: quando a IA generativa falha (`available=False`), o log agora registra o MOTIVO real (timeout / conexao / erro HTTP / resposta invalida) e o MODELO que o app usou. `GenerativeResult` ganhou os campos `error` e `model`; `generate_reply_and_route` os preenche a partir do `OllamaResult`; e `_handle_generative_turn` loga em WARNING "IA LOCAL FALHOU modelo=... motivo=..." (ou INFO com modelo/decisao no sucesso). Serve para descobrir por que o generativo so caia na fala de seguranca. Sem migration. Suite: 101 testes OK.
- REMOCAO TOTAL DA IA do sistema (a pedido, para recomecar do zero depois). Removidos: modulo `ai_engine/` inteiro (client/services/prompts/fallbacks/attendant/apps), integracao Ollama, telas de Automacao (IA de atendimento, Regras de atendimento) e Atendente Virtual (views, forms `AutomationAiTestForm`/`AutomationRuleForm`/`AiAttendantConfigForm`, templates, CSS `automation_ai.css`/`automation_rules.css`, rotas `automation-ai`/`automation-rules`/`ai-attendant-settings`, itens de nav "Automacao" e "Atendente Virtual"), comando `seed_ai_sector_rules`, settings do Ollama (`OLLAMA_*`) e `.env.example`, o app `ai_engine` do `INSTALLED_APPS`, o disparo da IA no `save_incoming_message` (`wapi/services.py`) e o parametro `is_ai` de `save_outgoing_*`. Models removidos: `AiAttendantConfig` e `AutomationRule` (tabelas apagadas); campos removidos: `Conversation.ai_state`/`ai_turns` e `Message.is_ai` (migration `0018`). As views `conversation-transfer`/`take`/`close` deixaram de mexer em `ai_state`. Testes de IA removidos; `ConversationTransferViewTests` mantida sem assercoes de `ai_state`. Nada mais depende de IA. Intactos: Conversas, Contatos, Setores, Atendentes, W-API (recebimento/webhook/envio/midia), notificacoes. Suite: 67 testes OK. Para limpar o VPS: parar/desabilitar o Ollama (`sudo systemctl disable --now ollama`), opcional `ollama rm qwen2.5:1.5b qwen2.5:3b`, e no `.env` remover as linhas `OLLAMA_*`; deploy normal com `git pull` + `migrate` (aplica 0018) + restart.
- Conversas: agora e UM UNICO CHAT POR PESSOA/GRUPO (padrao WhatsApp), com todo o historico num so lugar e DIVISORIAS marcando os atendimentos. Antes, ao encerrar, a proxima mensagem criava uma nova Conversation (chats "picotados"). Mudancas: `resolve_conversation_for_context` (`wapi/services.py`) deixou de excluir `status='closed'` e passou a SEMPRE reusar a mesma conversa do contato/grupo/LID; ao reusar uma conversa fechada, `_reopen_for_new_service` insere a divisoria "Novo atendimento iniciado" e reabre (`status='open'`). Novo tipo `Message.message_type='system'` (migration `0019`) para as divisorias, criadas por `save_system_message` (nao viram "ultima mensagem" da lista). `conversation_close_view` passou a inserir a divisoria "Atendimento encerrado" e a limpar tambem o `sector` (novo atendimento comeca do zero). Front: `buildMessageEl` (`conversations.html`) renderiza `kind='system'` como pilula centralizada `.conv-divider` (sem balao/horario); CSS `conversations.css?v=19`. Testes atualizados/novos em `ConversationTransferViewTests` (reuso da mesma conversa com divisoria, encerrar com divisoria + limpa setor, serializacao do tipo system). Suite: 68 testes OK. (Chats antigos ja picotados serao unificados por um comando de merge, na proxima etapa.)
- Conversas: novo comando `merge_contact_conversations` que UNIFICA os chats ja picotados (do comportamento antigo de fork) em um unico chat por pessoa/grupo. Agrupa por contato (ou por external_id+chat_type quando sem contato), escolhe a conversa mais antiga como canonica, reatribui as mensagens das demais preservando a ordem cronologica, insere uma divisoria "Novo atendimento iniciado" no limite de cada atendimento antigo e apaga as conversas vazias; o estado atual (status/setor/atendente/unread) vem do atendimento mais recente. Dry-run por padrao; `--apply` executa (fazer backup do db.sqlite3 antes). Testes `MergeContactConversationsTests` (dry-run nao altera; --apply unifica com divisoria na ordem certa). Suite: 70 testes OK. Sem migration.
- Docs: revisao de consistencia apos as ultimas mudancas. `CONTEXTO.md`: cache-busting atualizado para `conversations.css?v=19`; endpoints `conversation-take`/`conversation-close` adicionados a lista da secao 5; "Ja concluido" passou a citar ciclo de atendimento (assumir/encerrar), chat unico com divisorias + `merge_contact_conversations`, e a remocao total da IA. `WAPI_MEDIA_INTEGRATION.md`: tipo `system` (divisoria) incluido na lista de `message_type`. Verificado que nao ha sobras de OLLAMA/ai_engine fora das notas de "removido" (README, settings, .env.example limpos).
- IA/GPT (base): criada a estrutura para integrar a API do OpenAI (GPT), recomecando a IA do zero agora via API (nao mais Ollama local). Novo model singleton `OpenAiConfiguration` (`api_key`, `model` padrao `gpt-4.1-nano`, `enabled`, migration `0020`) que guarda a API Key NO BANCO (nunca no codigo; nao e reexibida apos salva, mesmo padrao do token da W-API), com `resolved_api_key()`/`resolved_model()` caindo para env (`OPENAI_API_KEY`/`OPENAI_MODEL`) quando vazio. Novo cliente `gpt/client.py` (modulo comum, como `wapi/`) usando so `urllib` (sem pacote pip novo): `chat_completion(messages, model, temperature, max_tokens, timeout)` e `test_connection()` (chamada minima que valida chave/modelo/creditos), com erros amigaveis (401->chave recusada, 429/quota->sem creditos, etc.) e log seguro `beezap.gpt` (nunca expoe API Key/corpo/traceback). Nova tela "Inteligencia (IA)" (item na barra lateral, so ADM): view `openai_settings_view`, rota `configuracoes/ia/` (`openai-settings`), form `OpenAiConfigurationForm` (API Key em PasswordInput, select de modelo `gpt-4.1-nano`/`gpt-4o-mini`/`gpt-4.1-mini`/`gpt-4o`, checkbox liga/desliga), template `accounts/openai_settings.html` + CSS proprio `static/css/openai_settings.css` (escopo `.openai-settings-page`), com card de status (API Key/modelo/ligada) e botao "Testar conexao". Settings novas `OPENAI_BASE_URL`/`OPENAI_API_KEY`/`OPENAI_MODEL`/`OPENAI_TIMEOUT` (+ `.env.example`). A IA vem DESLIGADA por padrao e ainda nao esta ligada a nenhum fluxo (recepcao/resposta automatica): esta etapa monta so a base + o cadastro/validacao da chave; o comportamento vem nas proximas etapas. `manage.py check` OK; suite 70 testes OK.
- IA/GPT: contador de consumo de tokens na tela Inteligencia (IA), para controle de gasto. O OpenAI devolve `usage` (prompt/completion/total) em cada resposta; `gpt/client.py` extrai (`_extract_usage`) e chama `OpenAiConfiguration.record_usage` (soma atomica com `F()`, segura para chamadas concorrentes; o contador nunca derruba a resposta do GPT). Novos campos no model (migration `0021`): `total_requests`, `total_prompt_tokens`, `total_completion_tokens`, `total_tokens`, `usage_since`, `last_used_at`; `reset_usage()` zera tudo. `GptResult` passou a expor `prompt_tokens`/`completion_tokens`/`total_tokens`. A tela ganhou o card "Consumo de tokens" (total, entrada, saida, nº de chamadas, "contando desde"/"ultimo uso") com botao "Zerar contador" (`form_type=reset-usage`); numeros formatados em pt-br (`_fmt_int`, ex.: 1.234.567). O botao "Testar conexao" tambem conta (gasto minimo). CSS `openai_settings.css?v=2`. `check` OK; suite 70 testes OK.
- IA/GPT: atendente virtual (recepcao/triagem) — a IA faz o PRIMEIRO atendimento de conversas DIRETAS sem setor/atendente. Novo `gpt/attendant.py`: monta o contexto automaticamente (prompt/persona editavel + data/hora p/ saudacao + setores [nome+descricao] + atendentes [nome+setor] + regra de formato JSON + ultimas ~5 trocas/ate 10 mensagens cliente<->IA terminando na mensagem atual), chama o GPT com `response_format=json_object` e decide via `{"mensagem","setor","atendente"}`: atendente casado -> assume + setor dele + `open`; setor casado -> setor + `pending` sem atendente; nenhum -> envia a fala e incrementa `ai_turns`. Ao atingir `max_turns` (padrao 3) sem decidir, encaminha ao `fallback_sector` (ou setor "Geral"; senao deixa `pending` sem setor) avisando o cliente. Cada encaminhamento insere divisoria "Encaminhado para ... pela IA". Guardas: pula se desligada/sem chave/grupo/fechada/ja roteada/humano ja respondeu no atendimento (out com `is_ai=False` apos a ultima divisoria); lock por conversa. Roda em background (thread), nunca trava o webhook. Ligacao: `save_incoming_message`/`ingest_wapi_payload(payload, trigger_ai=True)` disparam `handle_incoming_for_ai_async` no webhook AO VIVO; o comando `sync_wapi_events_to_conversations` chama com `trigger_ai=False` (nao responde mensagens antigas). Models (migration `0022`): `OpenAiConfiguration.instructions/max_turns/fallback_sector`, `Conversation.ai_turns` (reset em transferir/encerrar/reabrir), `Message.is_ai`. `save_outgoing_message`/`save_outgoing_text_message` ganharam `is_ai`. Cliente `chat_completion` ganhou `response_format`. Tela ampliada: prompt editavel, limite de respostas, setor de fallback e painel "O que e enviado para a IA" (prompt+setores+atendentes+nota do historico) para transparencia; CSS `openai_settings.css?v=3`. IA vem DESLIGADA por padrao. Testes: `AiAttendantFlowTests` (setor, atendente, esclarecer, fallback no limite, e guardas grupo/desligada/ja-roteada/humano). `check` OK; suite 78 testes OK.
- Docs/IA: deixada explicita a regra de que TODA alteracao vai ao Git com commit + push e mensagens em PORTUGUES (PT-BR) — `GIT.md` (secao 1) e `CODEX_PADROES.md` (Padrao de commit). Confirmado e travado com teste que a IA NAO interfere quando ha atendimento humano: novos testes `test_skips_when_attendant_assigned` (atendente assumiu) e `test_skips_when_closed` (atendimento encerrado) em `AiAttendantFlowTests` — a IA so recepciona conversas diretas sem atendente/setor e nao fechadas (comportamento ja garantido pelas guardas de `_should_handle`; nenhuma mudanca de logica foi necessaria). Suite 80 testes OK.
- IA/GPT: ao desistir (atingir `max_turns` sem identificar setor), a IA agora SEMPRE avisa o cliente antes de transferir — nunca em silencio. `_handoff_to_fallback` passou a mandar sempre a mensagem `HANDOFF_NOTICE` ("Desculpe, nao consegui entender bem a sua solicitacao. Vou pedir para um de nossos atendentes falar com voce...") em vez de reaproveitar a ultima pergunta do GPT (ou nada), e so entao encaminha para o `fallback_sector` (ou setor "Geral"). Sem fallback, avisa e deixa `pending` sem setor mantendo `ai_turns` no maximo (nao repete o aviso nem fica re-tentando). Testes: `test_fallback_after_max_turns` reforcado (aviso enviado = HANDOFF_NOTICE) e novo `test_fallback_without_sector_announces_and_waits`. Suite 81 testes OK.
- IA/GPT: a IA agora recebe HA QUANTO TEMPO foi a mensagem anterior, para poder se reapresentar quando faz tempo (a instrucao do prompt "se apresente de novo dependendo do tempo" antes nao funcionava — o modelo nao recebia horarios). Nova `_time_since_previous_text(conversation)` (primeira mensagem / ha poucos minutos / ha X hora(s) / ha X dia(s) — nova conversa) injetada no `build_system_prompt` (novo parametro `context_note`) junto da data/hora atual. Painel "O que e enviado para a IA" atualizado (item 4 cita hora atual + tempo desde a ultima msg). Teste `test_time_since_previous_text`. Suite 82 testes OK.
- IA/GPT: regras operacionais reforcadas no prompt de sistema apos teste real (saudacao "Ola" longa; assunto sem setor virou resposta inventada em vez de ir pro Geral). Nova `_operational_rules(config, greeting)` anexada automaticamente ao `build_system_prompt`: (1) ser BREVE (1-2 frases, tom WhatsApp); (2) iniciar a 1a mensagem com a saudacao do horario ("Boa noite!"), nao usar so "Ola" nem repetir a saudacao; (3) nao resolver o assunto nem inventar informacoes/precos/prazos/links/procedimentos; (4) encaminhar para o setor geral/fallback quando o pedido nao se encaixa em nenhum setor especifico (ex.: vaga de emprego) em vez de responder o conteudo. Isso resolve o caso do cliente perguntando de vaga (sem setor de RH): agora vai pro Geral em vez de inventar procedimento. Teste `test_system_prompt_rules`. Suite 83 testes OK.
- IA/GPT: painel de DIAGNOSTICO "Ultima chamada a IA" na tela Inteligencia (IA), mostrando o conteudo COMPLETO da ultima chamada real ao GPT — tudo que foi enviado (prompt do usuario + contexto automatico) e tudo que voltou (resposta crua). Novos campos `OpenAiConfiguration.last_request/last_response/last_exchange_at` (migration `0023`) gravados por `record_last_exchange` dentro de `chat_completion` (bloco finally, nunca derruba a resposta; captura sucesso, erro HTTP, sem conexao e resposta nao-JSON). Nunca contem a API Key (ela vai so no header). Serve para o ADM entender exatamente o que e (e o que nao e) enviado. CSS `openai_settings.css?v=4` (`.debug-text` com rolagem). Teste `test_records_last_exchange` (urlopen mockado). Suite 84 testes OK.
- IA/GPT: a pedido, as REGRAS DE COMPORTAMENTO sairam do codigo (auto-injetadas) e foram para o PROMPT EDITAVEL. `DEFAULT_INSTRUCTIONS` reescrito completo (persona + brevidade + saudacao do horario so na 1a msg / nao repetir / nao usar so "Ola" + apresentar-se / reapresentar apos tempo + encaminhar ao setor geral quando nada especifico se encaixa + nao inventar/nao pedir dados sensiveis). `build_system_prompt` deixou de injetar `_operational_rules` (removida); agora anexa automaticamente SO os dados dinamicos (data/hora + saudacao, tempo desde a ultima msg, setores, atendentes, qual e o setor geral/curinga) e a regra de formato JSON (obrigatoria). Tela ganhou botao "Restaurar prompt padrao" (JS preenche o textarea com o default) e ajuda atualizada; CSS `openai_settings.css?v=5`. Testes: `test_default_prompt_has_behavior_rules` e `test_system_prompt_auto_parts` (substituem `test_system_prompt_rules`). Suite 85 testes OK.
- IA/GPT: `build_history` passou a usar SO as mensagens do ATENDIMENTO ATUAL (apos a ultima divisoria de sistema), em vez das ultimas 10 da conversa inteira. Assim, ao Encerrar/reabrir, a IA comeca com contexto limpo, sem arrastar mensagens de atendimentos/testes anteriores (diagnostico real mostrou historico poluido confundindo o modelo). Teste `test_history_scoped_to_current_segment`. Suite 86 testes OK. (Observacao de diagnostico: com `gpt-4.1-nano` o modelo ignora instrucoes claras do prompt — ex.: saudar com "Boa noite" e ser breve —; recomendado `gpt-4o-mini` para aderencia; o roteamento ao setor Geral ja funciona.)
- DASHBOARD (bug do gráfico RESOLVIDO — localizacao pt-BR): a causa real de os numeros/datas do grafico empilharem no canto era o `LANGUAGE_CODE='pt-br'` — o Django imprimia as porcentagens de posicao com VIRGULA decimal (`{{ 6.0 }}` -> `6,0`), gerando `style="left: 6,0%"` (CSS invalido), entao `left/top` eram ignorados e os spans caiam pro fluxo. So aparecia no VPS (locale pt-BR aplicado); a polilinha/area funcionavam por serem geradas em Python (sempre com ponto). Fix: envolver o grafico em `{% localize off %}` (com `{% load l10n %}`), forcando ponto decimal nos `left/top`/gridlines. Verificado sob pt-BR: `left: 6.0%` (ponto). (Diagnostico longo tambem revelou: sem CDN/proxy_cache; DEBUG=False cacheia templates -> todo deploy TEM que reiniciar o gunicorn de verdade e confirmar PID/etimes novo.)
- DASHBOARD (a prova de cache): diagnostico mostrou que o servidor ja entregava o CSS/HTML corretos (HEAD certo, `curl` do dashboard.css com as regras novas), mas o navegador/CDN continuava aplicando o CSS antigo (numeros/datas do grafico caindo no canto porque `.dc-val`/`.dc-date` nao recebiam `position:absolute`). Solucao: o estilo do grafico "Atendimentos por dia" foi EMBUTIDO num `<style>` dentro do `dashboard.html` (depois do link do CSS, entao prevalece). Como o HTML vem sempre fresco do servidor, o estilo nunca fica em cache velho — imune a cache de navegador/CDN. As mesmas regras seguem em `dashboard.css` (para outras telas). Sem migration.
- DASHBOARD (grafico refeito de vez): o "Atendimentos por dia" passou a ter ZERO texto dentro do SVG — a linha, a area e a grade ficam no SVG (coordenadas em %, `preserveAspectRatio=none` + `vector-effect=non-scaling-stroke`), e os NUMEROS, as DATAS e os PONTOS sao HTML posicionados por % por cima (`.daychart`). Isso elimina de vez o corte do rotulo do valor maximo (que virava "1"/"4" solto) e os caracteres sobrepostos no canto — problemas que eram do texto do SVG. Verificado: valores [0,2,0,5,8,14,7], datas 03/07..09/07, pico (14) no topo. `dashboard.css?v=5`. Sem migration.
- DASHBOARD (fix do corte): o rótulo do valor mais alto do gráfico (ex.: "14") aparecia cortado (virava "1") porque o ponto máximo colava na borda de cima do SVG. Adicionada FOLGA no topo (top=55, viewBox 0 0 720 260), então nenhum rótulo é cortado; verificado que os valores nos pontos = [0,2,0,5,8,14,7], datas = 03/07..09/07 e eixo Y = 0/7/14 batem. `dashboard.css?v=4`. Sem migration.
- DASHBOARD: gráfico "Atendimentos por dia" refeito como gráfico de linha DE VERDADE — eixo Y com linhas de grade e valores (0 / metade / máximo), área preenchida (degradê) sob a linha, pontos com o valor do dia em cima e a data embaixo. A linha agora acompanha claramente os valores (a grade serve de referência). `build_dashboard_context` passou a devolver `chart_area` e `chart_gridlines`. `dashboard.css?v=3`. Sem migration.
- DASHBOARD (ajuste visual): gráfico "Atendimentos por dia" refeito — viewBox menor (0 0 700 250, mais baixo), margens internas (x 55–645, y 55–205) para os rótulos não serem cortados nem gerar artefato no canto, com o VALOR de cada dia em cima do ponto e a DATA embaixo. "Atendimentos por setor": donut + legenda agora CENTRALIZADOS no card (`.donut-panel` flex coluna + `.donut-wrap` flex centralizado), sem ficar espremido à esquerda. `dashboard.css?v=2`. Sem migration.
- DASHBOARD com DADOS REAIS: os cards (Conversas ativas = nao fechadas; Novas conversas = criadas nos ultimos 7 dias; Atendimentos finalizados = fechadas; Tempo medio de resposta = 1a resposta do atendente apos a 1a msg do cliente) e os graficos (Atendimentos por dia = ultimos 7 dias pela data da ultima mensagem; Atendimentos por setor = donut com distribuicao real; Atendimentos em andamento = conversas abertas) agora vem do banco (`build_dashboard_context`), nao mais fixos. Removidos os ATALHOS do topo (Nova conversa / Fila / Relatorios / Configuracoes), a pedido — o dashboard tem so os indicadores. Data de hoje no chip. Novo comando `seed_demo_data` (`--no-clear`) que LIMPA o conteudo (conversas/mensagens/contatos + atendentes/setores ficticios) e popula 5 setores, 5 atendentes (um por setor) e 36 conversas distribuidas nos ultimos 7 dias (10 em atendimento, 8 aguardando, 18 finalizadas), PRESERVANDO admin/configuracoes/permissoes — os mesmos dados aparecem no Dashboard e em Conversas. Testes `DashboardTests` (seed + render; dashboard vazio ok). Suite 111 testes OK. Sem migration.
- PERMISSOES/Grupos: botao "Remover" por grupo na aba Grupos. A lista de grupos e dirigida por MENSAGEM RECEBIDA (um grupo aparece quando chega mensagem dele; nao vem do get-all-groups da W-API), entao grupos antigos/onde o numero saiu podem ficar la. O X remove a conversa do grupo (com confirmacao; apaga o historico via cascade; `form_type=group-remove`, AJAX). Se chegar nova mensagem do grupo, ele volta a aparecer. `permissions.css?v=5`. Sem migration.
- PERMISSOES/Grupos (diagnostico do nome): o "Atualizar nomes" passou a informar o resultado real — quantos nomes foram atualizados, e quando NAO acha (0), orienta a digitar o nome no campo (distingue "W-API nao retornou grupos" de "nome deste grupo nao veio"). O endpoint `conversation-sync-groups` devolve `updated` + `total_groups`. Novo comando de diagnostico `inspect_wapi_groups` (`--full`) que mostra a resposta crua de get-all-groups da W-API e o nome extraido de cada grupo (para descobrir, no servidor, por que um grupo nao tem nome — grupo ausente na resposta ou nome em campo nao lido pelo parser). Sem migration.
- PERMISSOES/Grupos: nome do grupo agora e EDITAVEL inline na aba Grupos (campo de texto por grupo, salva ao sair do campo via AJAX `form_type=group-name` -> `Conversation.name`), com o JID mostrado como subtitulo. Assim, se a W-API nao devolver o nome (grupo aparecia como "Grupo 120363144038483540"), o admin digita o nome certo ali mesmo; o botao "Atualizar nomes" continua para puxar automaticamente da W-API. `permissions.css?v=4`. Sem migration; suite 109 OK.
- PERMISSOES (UX): as telas de Botoes do perfil, Personalizar usuario e Grupos passaram a SALVAR AUTOMATICAMENTE ao clicar em cada toggle/chip (fetch AJAX; `permissions_view` responde JSON quando `X-Requested-With`), com toast de confirmacao — removidos os botoes "Salvar perfis/usuario/grupos". Na aba Grupos, novo botao "Atualizar nomes" (chama `conversation-sync-groups` -> `sync_group_names`, busca o nome real do grupo na W-API) para o grupo deixar de aparecer como o JID cru ("Grupo 12036..."). `permissions.css?v=3` (cabecalho flex + hint). Sem migration; suite 109 testes OK.
- CONVERSAS/PERMISSOES: separacao dos chats + aba de Grupos + escopo de historico + acentos. (1) A tela Permissoes virou ABAS: "Botoes do perfil" (toggles por perfil/usuario, agora com "Ver conversa inteira") e "Grupos" (lista os grupos detectados e libera cada um por setor e/ou usuario -> model `GroupAccess`). (2) SEPARACAO DAS CONVERSAS: o usuario comum deixou de ver tudo — `visible_conversations`/`can_see_conversation` (`accounts/permissions.py`): admin ve tudo; nao-admin ve diretas atribuidas a ele OU do setor dele, e grupos liberados para o setor/usuario. Aplicado na lista e contadores (`conversations_view`, `conversation_list_view`) e nas acoes (`messages/send/take/transfer/close/send-media` -> 403 se nao pode ver). Usuario novo/sem setor fica "zerado". (3) HISTORICO por atendimento vs completo (`full_history` em Role/UserMenuPermission, escolha por perfil e por usuario; padrao = so o atendimento atual, apos a ultima divisoria) aplicado em `conversation_messages_view`. (4) ACENTOS PT-BR corrigidos nas telas/nav: "Permissoes"->"Permissões", "Configuracoes"->"Configurações", "Inteligencia"->"Inteligência", "Opcoes"->"Opções", "Usuario"->"Usuário" (labels do menu e titulos das telas de config). Migration `0027` (GroupAccess + full_history). Testes `ConversationVisibilityTests` (admin ve tudo, usuario so o setor dele, grupo por setor/usuario, gate 403, escopo de historico) + ajuste de `test_attendant_can_take_conversation` (conversa no setor do atendente). Suite 109 testes OK.
- PERMISSOES: nova tela "Permissoes" (so ADM, item na barra lateral, rota `permissoes/`) onde o admin define, de forma visual (toggles/switches com icone), quais BOTOES do menu cada PERFIL ve e acessa, e pode personalizar um USUARIO especifico. Nao e so visual: as telas passaram a ser gateadas por feature (`require_feature`/`user_can_access` em `accounts/permissions.py`), entao esconder o botao tambem bloqueia a URL. Modelos `RoleMenuPermission` (por perfil) e `UserMenuPermission` (override por usuario), migration `0026`. O ADMINISTRADOR tem sempre acesso total (nao editavel). Padrao dos demais: Conversas + Contatos (sem Dashboard). Menu reescrito: `build_nav_items(user, ...)` monta os itens conforme a permissao; itens placeholder (Atendimentos/Campanhas/Relatorios) REMOVIDOS; quem nao tem Dashboard cai na 1a tela disponivel pos-login (`first_landing_url_name`, `dashboard_view` redireciona). Checagens `role != 'adm'` das telas (Configuracoes/IA/Chatbot/Atendentes/Setores) trocadas por `require_feature`. Testes `MenuPermissionsTests` (padrao do usuario, gate 403, admin ve tudo, concessao por perfil, override e reset por usuario) + ajuste de `test_valid_initial_password_change_unlocks_user` (usuario cai em Conversas, nao Dashboard). Suite 103 testes OK.
- SETORES: removida a faixa de dica ("Um atendente pode ficar em varios setores...") no topo da tela, a pedido; os botoes Novo setor / Salvar organizacao alinhados a direita (`.sec-actions-bar` flex-end). `sectors.css?v=3`.
- SETORES/ATENDENTES: (1) o mesmo atendente pode ficar em VARIOS setores de forma facil. Antes, ao alocar um atendente ele SUMIA da coluna "Atendentes disponiveis" e arrastar entre setores MOVIA (tirava do de origem), efetivamente limitando a 1 setor. Agora o atendente fica SEMPRE disponivel (arraste para quantos setores quiser, ou use "+ Adicionar atendente"); os cards dentro do setor deixaram de ser arrastaveis (removem so pelo x); e cada card na coluna mostra "em N setores". (2) Selo "Administrador"/"Admin" para identificar o admin nas telas de Atendentes (lista) e Setores (coluna de disponiveis, cards nos setores e modal de adicionar) — `attendants_data` passou a expor `is_admin`. Frontend + CSS (`sectors.css?v=2`, `attendants.css?v=2`); backend so adiciona `is_admin` ao contexto. Suite 97 testes OK.
- ATENDENTES/Admin como atendente: o ADMINISTRADOR passa a virar atendente AUTOMATICAMENTE (aparece em Atendentes) e a fazer parte de TODOS os setores, para conseguir usar o botao "Assumir" em qualquer fila sem precisar criar/logar uma conta separada de atendente. Antes, "Assumir" com o admin logado dava "Esta conta nao possui perfil de atendente". Implementado por sinais (`accounts/signals.py`, registrados em `AccountsConfig.ready`): `post_save` de User adm garante o `Attendant` (nome do usuario, `must_change_password=False`) e o inclui em todos os setores; `post_save` de Sector inclui todos os admins no novo setor. Backfill dos admins/setores ja existentes na migration `0025`. `conversation_take_view` provisiona o perfil na hora se faltar (rede de seguranca) e `sectors_save_organization_view` re-inclui os admins apos o `set()` do arrastar-e-soltar (senao seriam removidos das filas nao listadas). Corrigido tambem um efeito colateral: a edicao de atendente forcava `role=usuario` — agora NAO rebaixa um adm que tambem atua como atendente. Testes `AdminAttendantTests` (admin ganha atendente em todos os setores, novo setor inclui o admin, admin assume conversa) e ajuste de `test_duplicate_email_is_rejected` (compara com a contagem anterior, ja que o admin conta como atendente). Suite 97 testes OK.
- ATENDIMENTO/Chatbot (ajustes): (1) corrigidos os botoes SEM ESTILO na tela do chatbot ("Salvar modo", "Adicionar opcao", "Salvar configuracao") — `.primary-button`/`.secondary-button` so tinham estilo escopado nas telas W-API/IA; adicionadas as regras escopadas em `.chatbot-settings-page` (`chatbot_settings.css?v=2`). (2) Novo botao **"Preencher automaticamente"**: cria uma opcao de menu para CADA setor cadastrado (rotulo = nome do setor) e preenche saudacao/intro/confirmacao/invalida/handoff com o texto padrao, deixando so ajustar e salvar — dados expostos ao JS via `json_script` (`sectors-data`/`defaults-data`); pede confirmacao antes de sobrescrever e avisa se nao houver setores. Sem migration; sem mudanca de backend (view so passa `sectors_json`).
- ATENDIMENTO/Chatbot de menu (sem IA): adicionada uma segunda opcao de primeiro atendimento, por MENU numerado fixo (sem custo de IA), como alternativa ao atendente virtual GPT. Fluxo: no 1o contato de uma conversa direta sem setor/atendente, envia a saudacao + o menu; a cada resposta interpreta o numero digitado — opcao valida avisa o cliente (mensagem de confirmacao com `{setor}`) e encaminha para o setor da opcao (`pending`); opcao invalida reexibe o menu contando a tentativa; apos `max_attempts` tentativas invalidas avisa que nao entendeu e encaminha para um atendente (setor de fallback, ou deixa aguardando). Novo MODO MESTRE de primeiro atendimento (`MenuBotConfiguration.mode`: `off`/`menu`/`ai`) como FONTE UNICA da verdade de qual motor atua — o webhook (`_maybe_trigger_reception` em `wapi/services.py`, ex-`_maybe_trigger_ai`) dispara IA, chatbot ou nada conforme o modo; a guarda da IA (`gpt/attendant._should_handle`) passou a ler o modo em vez de `OpenAiConfiguration.enabled` (que ficou vestigial, mantido em sincronia por compatibilidade). Novos models `MenuBotConfiguration` (singleton) + `MenuOption` (rotulo + setor por linha) e migration `0024` (data-migra: se a IA estava ligada, `mode` nasce `ai`, senao `off`). Novo modulo comum `chatbot/handler.py` (espelha `gpt/attendant.py`: roda em thread, lock por conversa, reusa `Conversation.ai_turns` como contador de tentativas, as divisorias e a guarda de humano-assumiu; divisoria "Encaminhado para o setor X pelo chatbot"). REORGANIZACAO das telas de config em ABAS: a area Configuracoes agora tem uma barra de abas [WhatsApp] [Atendimento] (partial `_settings_tabs.html` + `settings_tabs.css`); a aba Atendimento tem o seletor de modo no topo e duas sub-abas [Chatbot de menu] [Inteligencia (IA)]. A config da IA foi MOVIDA para a sub-aba IA (o item solto "Inteligencia (IA)" saiu da barra lateral; a ativacao deixou de ser o checkbox "Ativar" e virou o seletor de modo). Nova tela `atendimento_view` (`configuracoes/atendimento/`, `chatbot_settings.html` + `chatbot_settings.css`, escopo `.chatbot-settings-page`) com editor de opcoes (adicionar/remover/renumerar por JS) e previa do menu; endpoint `atendimento_set_mode_view` (`configuracoes/atendimento/modo/`) salva o modo. So ADM. O chatbot vem DESLIGADO por padrao. Testes: `MenuBotFlowTests` (menu no 1o contato, opcao valida->setor, invalida->reexibe/conta, handoff no limite, e guardas modo/grupo/ja-roteada/humano) e `AiAttendantFlowTests` ajustada para ativar por `mode='ai'`. Corrigido tambem o teste pre-existente `test_history_scoped_to_current_segment`, fragil a baixa resolucao do relogio no Windows (empatava `created_at` da divisoria com a mensagem seguinte). Suite 94 testes OK.
- IA/CHATBOT (encaminhamento): ao encaminhar, a conversa agora fica sempre AGUARDANDO na fila do SETOR (`pending`, SEM atribuir a uma pessoa) — o time do setor e notificado e alguem clica em Assumir (ai vira `open`/em atendimento). Antes, quando a IA casava um atendente (`_route_to_attendant`), ela atribuia a pessoa e marcava `open` (aparecia como "em atendimento"); agora `_route_to_attendant` vai para o SETOR do atendente citado, sem atribuir. Alem disso, a IA e o chatbot **deixaram de inserir a divisoria** "Encaminhado para ... pela IA/chatbot": o encaminhamento e parte do MESMO atendimento, entao o atendente que assume passa a ver TODO o historico (inclusive a conversa com a IA/menu) — antes a divisoria criava um novo segmento e, com o escopo de historico do atendente (so o atendimento atual), o historico com a IA ficava escondido. Divisorias de Encerrar/reabrir seguem intactas (essas sim sao limites de atendimento). Testes `AiAttendantFlowTests`/`MenuBotFlowTests` atualizados (encaminhamento -> setor/pending/sem atendente/sem divisoria). Suite 111 testes OK. Sem migration.
- CONVERSAS/Aguardando: "Aguardando" saiu dos chips de filtro e virou um BADGE amarelo PULSANTE ao lado dos botoes do topo (Som/Notificacoes/Atualizar), mostrando a contagem de conversas aguardando (`counts["aguardando"]`, ja escopado pelo setor do usuario). Pulsa (some quando zero) para todos os atendentes do setor; clicar filtra a lista so nos aguardando (aplica `status=aguardando`), clicar de novo volta para Todas. View: `filter_chips` exclui `aguardando` e passa `waiting_count`; template ganhou `.conv-waiting-badge` (`data-waiting-badge`) + JS (`updateWaitingBadge`, clique) ; `conversations.css?v=20` (badge + animacao `conv-waiting-pulse`, respeita prefers-reduced-motion). Suite 111 testes OK. Sem migration.
- CONVERSAS (ciclo/UX): varios ajustes ligados. (1) filtro "Em atendimento" renomeado para "Conversando". (2) ENCERRAR corrigido: antes o `conversation_close_view` limpava atendente E setor, e como a visibilidade exige atribuicao/setor, a conversa fechada SUMIA da vista do atendente (nem ia pros Finalizados). Agora o encerrar MANTEM o `assigned_attendant` (limpa so o setor), entao o atendente que fechou continua vendo em Finalizados; `_reopen_for_new_service` (proxima msg do cliente) volta a ZERAR atendente+setor (a nova conversa volta pra recepcao/fila). (3) HISTORICO escopado agora usa a ultima divisoria "Novo atendimento iniciado" (nao qualquer divisoria) — assim um chat finalizado (ou recem-encaminhado pela IA) mostra TODO o atendimento (cliente + IA/menu), nao so a divisoria. (4) Botoes do painel por status (`fillInfo` le `contact.status`): em FINALIZADO esconde Assumir, Encerrar e a caixa de transferir (so leitura). (5) Lista destaca "em conversa comigo": conversas atribuidas ao usuario logado ganham `.conv-item-mine` (borda azul + fundo, label "Em conversa com voce"); flag `mine` no `_serialize_conversation_item(conv, request.user)`; regra 1 conversa = 1 atendente. `_serialize_contact_info` expoe `status` cru. `conversations.css?v=21`. Testes `ClosedConversationTests` + ajustes em `ConversationTransferViewTests`. Suite 113 testes OK. Sem migration.
- CONVERSAS (painel de info): corrigido o painel lateral que nao atualizava apos Assumir/Encerrar (os botoes continuavam, dava pra assumir/encerrar de novo indefinidamente). Extraida `updateServiceButtons(contact)` chamada tanto ao abrir a conversa quanto no sucesso de assumir/encerrar/transferir (via `applyContactInfo`), lendo `contact.status` + `contact.mine`. Regra dos botoes: finalizado (closed) = so leitura (esconde Assumir/Encerrar/transferir); ja atribuida a MIM = esconde "Assumir" (so "Encerrar", pois eu ja sou o atendente); aguardando/de outro = mostra ambos. `_serialize_contact_info` passou a receber `request.user` e expor `mine`. Suite 113 testes OK. Sem migration; sem mudanca de CSS.
- CONVERSAS/Grupos: 3 ajustes. (1) Grupo nao e "atendimento": ao abrir um grupo, o painel esconde Assumir/Encerrar/Transferir (updateServiceButtons trata is_group) — vale para admin e atendente. (2) Como o grupo e um numero unico, a mensagem ENVIADA agora registra o atendente que mandou (`Message.sender_name` via `_current_attendant_name`; `save_outgoing_*` e `save_outgoing_message` ganharam `sender_name`) e o chat mostra o nome acima do balao tanto nas recebidas (participante) quanto nas enviadas (atendente, `.conv-msg-sender-me`); `_serialize_message` usa `sender_name` para mensagens `out`. (3) Grupos NAO entram mais em "Aguardando" (filtro `aguardando` restrito a `chat_type=private`), entao o badge/contador nao conta grupo. `conversations.css?v=22`. Testes `GroupConversationTests`. Suite 116 testes OK. Sem migration.
- CONVERSAS/Grupo: o nome do atendente agora vai TAMBEM no corpo da mensagem enviada ao grupo (antes so aparecia no nosso sistema; os participantes do grupo, no WhatsApp, nao sabiam quem falou). `conversation_send_view` prefixa `*<atendente>*` + quebra de linha no texto enviado a W-API quando `conversation.is_group`; o texto salvo no nosso chat continua SEM o prefixo (o nome ja aparece acima do balao, para nao duplicar). Direta nao leva prefixo. Teste `test_outgoing_group_message_records_sender_name` reforcado (verifica o prefixo no enviado e o texto limpo salvo). Suite 116 testes OK. Sem migration; sem mudanca de CSS.
- CONVERSAS (finalizados/destaque): (1) FINALIZADOS agora sao so os MEUS — uma conversa fechada so aparece para o atendente que a atendeu (por atribuicao), nao para o setor inteiro. Antes a visibilidade por setor deixava todos do setor verem os finalizados uns dos outros (aparecia "Finalizado (Eduarda)" pra Ana). `visible_conversations_q`: visibilidade por SETOR agora so vale para conversas NAO fechadas; fechada so por atribuicao (admin ve tudo). (2) O destaque AZUL ("em conversa comigo") virou exclusivo de conversas ATIVAS atribuidas a mim — flag `mine` passou a exigir `status != closed`; finalizada nao fica mais azul (label simplificado para "Finalizado"). Testes ajustados (`ClosedConversationTests`: finalizado com `mine=False`, label "Finalizado"). Suite 116 testes OK. Sem migration; sem mudanca de CSS.
- PERMISSOES/Perfis: nova aba "Perfis" na tela Permissoes (so ADM) onde o admin define, de forma VISUAL, o papel de cada pessoa (`adm`/`usuario`/`leitor`). Cada pessoa aparece como um cartao (avatar + nome + e-mail) com um seletor de 3 pilulas (👑 Administrador · 🎧 Usuario · 👁️ Leitor), a ativa colorida por perfil; clicar salva na hora (AJAX `form_type=profile-role`, atualizacao otimista com reversao em erro + toast). Guardas: o admin NAO pode alterar o proprio perfil (pilulas desabilitadas + selo "voce", evita se trancar fora) e ha a rede de seguranca "deve existir >=1 administrador". Promover a `adm` provisiona o atendente/setores pelo sinal existente. E o UNICO lugar da interface para trocar o papel (antes so via Django admin/shell). CORRIGIDA a armadilha da tela Atendentes: o edit NAO mexe mais no `role` (antes forcava `usuario` no editar, o que apagaria a escolha feita aqui). As abas foram reorganizadas para [Perfis][Botoes do perfil][Grupos] (chaves internas `people`/`botoes`/`grupos`; aba padrao = Perfis). Diagnostico do "leitor": hoje ele NAO restringe acoes — as views de conversa (enviar/assumir/encerrar/transferir/midia) so checam visibilidade, nunca o role, entao `leitor` com "Conversas" liberado interage igual ao `usuario` (o nome engana). Tornar `leitor` somente-leitura de verdade ou remove-lo ficou como decisao pendente. `permissions.css?v=6`. Testes `ProfileRoleTests` (troca de papel, promover a adm provisiona atendente, nao altera o proprio, rebaixa outro admin, role invalido, nao-admin 403). Suite 122 testes OK. Sem migration.
- DEPLOY: `deploy/deploy.sh` agora VERIFICA automaticamente o reinicio do gunicorn (trata a armadilha do DEBUG=False que cacheia templates na memoria dos workers). Guarda os PIDs do gunicorn (`pgrep -f beezap/venv/bin/gunicorn`) antes e depois do `systemctl restart`; se nao reciclaram (PIDs iguais ou nenhum processo), FORCA o reinicio de verdade (`stop` + `pkill` + `start`); se mesmo assim o gunicorn nao subir, ABORTA o deploy com erro (`exit 1` + `systemctl status`). Ao final imprime o `etimes` dos processos (deve ser poucos segundos). Assim a conferencia manual "os PIDs reciclaram?" que estava so na doc passou a ser automatica em todo `bash deploy/deploy.sh`. `docs/DEPLOY.md` (Fluxo de deploy padrao) atualizado. Sintaxe validada com `bash -n`. Sem mudanca de codigo Python; sem migration.
- SETORES (Geral padrao + scroll horizontal): (1) SETOR "GERAL" PADRAO — sempre existe, nao pode ser excluido nem renomeado, e TODOS os atendentes fazem parte dele por padrao. Novo `Sector.GENERAL_SECTOR_NAME='Geral'`, classmethod `Sector.ensure_general()` (cria e, ao criar, inclui todos os atendentes) e prop `is_general`. Migration de dados `0028` cria o "Geral" e faz backfill de todos os atendentes; novo sinal `post_save` de Attendant (`accounts/signals.py`) adiciona todo atendente novo ao Geral. Backend: `sectors_view` bloqueia excluir o Geral ("O setor Geral e padrao e nao pode ser excluido.") e trava o nome na edicao (so a descricao muda). Frontend (`sectors.html`, `sectors.css?v=5`): card do Geral sem botao de excluir, com selo "padrao", e campo nome readOnly no modal quando for o Geral. `gpt/attendant.py` e `chatbot/handler.py` passaram a usar `Sector.ensure_general()` (removidas as copias locais `_ensure_general_sector`). `_route_to_attendant` (IA) agora prefere um setor ESPECIFICO do atendente (nao o Geral) — como todos estao no Geral, `sectors.first()` alfabetico pegava "Geral" por engano. `seed_demo_data` preserva o Geral (nao apaga) e garante `ensure_general()`. (2) SCROLL HORIZONTAL na roda do mouse: na area de setores (`.sec-area`), girar a roda do mouse rola de LADO (converte deltaY em scrollLeft), exceto quando o cursor esta sobre uma lista de atendentes que ainda pode rolar na vertical (respeita o scroll interno do card). Tambem corrigido o selo "padrao"/nome do setor com o mesmo padrao anti-clipping do selo Admin (`.sector-name-text`). Testes `GeneralSectorTests` (ensure cria+inclui todos, atendente novo entra, nao exclui, nao renomeia, setor comum exclui) + ajustes nos setups que criavam "Geral" (agora `get_or_create`) e no `DashboardTests` (5 setores demo + Geral). Migration `0028`. Suite 136 testes OK.
- IA/CHATBOT (bug do handoff que nao encaminhava): ao DESISTIR da triagem (atingir o limite de turnos/tentativas sem identificar setor), quando NAO havia `fallback_sector` configurado E nao existia um setor "Geral", a conversa ficava `pending` SEM setor — invisivel para os atendentes e fora de qualquer fila (so o admin via). Na pratica, a IA avisava "vou pedir para um atendente" e a conversa nao ia para ninguem (exatamente o relato: cliente perguntou por um "Joao" inexistente, a IA alucinou "vou verificar", e no fim o handoff nao transferiu). CORRIGIDO: tanto `gpt/attendant._handoff_to_fallback` quanto `chatbot/handler._handoff` agora SEMPRE encaminham para um setor real — `fallback_sector`, um "Geral" existente, ou um "Geral" CRIADO na hora (`_ensure_general_sector`, `get_or_create`); nunca mais deixam a conversa orfa. Criar o "Geral" dispara o sinal que inclui os admins nele (aparece em "Aguardando Geral"). O guarda de topo da IA (`ai_turns >= max_turns`) tambem passou a sempre fazer o handoff. Ajuste no prompt padrao (`DEFAULT_INSTRUCTIONS`): se o cliente citar uma pessoa que NAO esta na lista de atendentes, a IA nao deve dizer que "vai verificar a disponibilidade" nem prometer retorno (reduz a alucinacao do "Joao"); a aderencia total depende do modelo (com `gpt-4.1-nano` a adesao e fraca — recomendado `gpt-4o-mini`). Testes: `test_handoff_creates_general_sector_when_no_fallback` e `test_handoff_routes_to_existing_general_when_no_fallback` (IA) e `test_handoff_creates_general_when_no_fallback` (chatbot); `test_fallback_without_sector_announces_and_waits` (que esperava a conversa ficar orfa) substituido. `docs/CONTEXTO.md` secoes 13 e 14 atualizadas. Suite 131 testes OK. Sem migration.
- SETORES (correcoes visuais/UX): (1) o selo "Admin" NAO aparecia ao lado de nomes GRANDES (ex.: "Eduarda Nunes") porque ficava DENTRO do `.sec-att-name`, que tem `overflow:hidden`/`text-overflow:ellipsis` — o nome longo "comia" o selo. Corrigido separando o nome (`.sec-att-name-text`, que agora e quem trunca) do selo, com `.sec-att-name` virando um flex row e `.sec-att-admin-badge` com `flex-shrink:0` (selo SEMPRE visivel). Ajustado nos 4 pontos de render (coluna de disponiveis + dentro do setor no template; `createSectorCard` + `renderAddList` no JS). (2) Arrastar um atendente para um setor onde ele JA esta (comum com o admin, que entra em TODOS os setores) nao fazia NADA, sem mensagem — agora mostra um toast "<Nome> ja esta neste setor." (no `drop` e no `addToSector`). `sectors.css?v=4`. Sem mudanca de Python; sem migration; suite (AdminAttendantTests) OK.
- PERMISSOES/Leitor SOMENTE LEITURA: o perfil `leitor` passou a ser DE FATO somente leitura — enxerga as telas liberadas (em "Botoes do perfil"), mas NAO executa nenhuma acao que altere dados. Novo helper central `is_read_only(user)` (`accounts/permissions.py`) e dois guardas em `views.py`: `deny_readonly_json` (endpoints AJAX -> 403 JSON) e `block_readonly` (telas de formulario -> 403). Aplicados em: enviar texto (`conversation_send_view`), enviar midia (`conversation_send_media_view`), assumir (`conversation_take_view`), encerrar (`conversation_close_view`), transferir (`conversation_transfer_view`), nomear contato (`conversation_name_contact_view`), sincronizar grupos (`conversation_sync_groups_view`), salvar organizacao de setores (`sectors_save_organization_view`), e no POST de Contatos/Atendentes/Setores/W-API/IA(GPT)/Chatbot/modo de atendimento. O que e so leitura (GET: abrir conversas/mensagens/listas) continua liberado. FRONTEND: `conversations_view`/`contacts_view` passam `read_only` ao template; em Conversas, `.conv-body.is-readonly` esconde o composer, a caixa de transferencia e os botoes Assumir/Encerrar e mostra a barra "👁️ Perfil somente leitura" (`conversations.css?v=23`); em Contatos somem "Novo contato" e as acoes Editar/Excluir. O admin continua escolhendo QUAIS botoes o leitor ve (aba Botoes do perfil) — o perfil garante que ali ele so visualiza. Testes `ReadOnlyLeitorTests` (ve conversas/mensagens; nao envia/assume/encerra/transfere/cadastra contato; pagina marca is-readonly). Verificado no navegador (Client) para leitor e usuario. Suite 129 testes OK. Docs: CONTEXTO secao 15 (nova subsecao "Perfil SOMENTE LEITURA") e pendencia da secao 10 atualizadas. Sem migration.
- LOGIN (bug de recuperacao de senha): ao pedir recuperacao de senha, a tela de codigo era renderizada com a URL do navegador em `/recuperar-senha/solicitar/` (a view `password_recovery_request_view` renderiza o proprio `login.html`). O formulario de login NAO tinha `action`, entao enviava para a URL ATUAL — ao fechar o modal e clicar em "Entrar", o POST caia de novo em `password_recovery_request_view`; como o POST do login carrega o campo `email`, o `PasswordRecoveryRequestForm` validava, reenviava o codigo e voltava para a tela de digitar o codigo (sintoma relatado). CORRIGIDO: `login.html` agora tem `action="{% url 'login' %}"` no `<form class="login-form">`, garantindo que o login sempre poste na rota `login` mesmo quando o template e servido a partir das URLs de recuperacao. So template; sem Python; sem migration. Verificado via Client: o form de login na pagina de recuperacao aponta para `/` (com prefixo vira `/beezap/`).
- CONVERSAS (sino de notificacoes agora e SOMENTE informativo): o botao do sino no topo da lista pedia/testava a permissao ao ser clicado. Bug relatado: com a permissao JA concedida (sino verde), clicar disparava `new Notification(...)` de teste que, em alguns navegadores, lanca erro (ex.: Chrome exige service worker) — o sino ficava vermelho e nao fazia mais nada, mesmo com o navegador continuando liberado. CORRIGIDO: o sino virou um INDICADOR nao clicavel (era `<button data-notif-toggle>`, agora `<span class="... conv-notif-indicator" role="img">`). Removidos o handler de clique, o `requestNotifyPermission()` e o `showDesktopTestNotification()`. Mantida a deteccao (`setNotifUI`) que so reflete o estado do navegador (verde=ativas, ambar=desativadas, vermelho=bloqueadas) e o auto-update via `navigator.permissions.onchange`. A permissao passa a ser concedida so pelo navegador (cadeado/Config. do site); os tooltips orientam isso. As notificacoes reais de nova mensagem (`showDesktopNotification`/`canNotify`) seguem intactas. CSS: `.conv-notif-indicator` (cursor default, sem hover) e removidos os `:hover` de `.off`/`.blocked`; `conversations.css?v=24`. So template/CSS; sem Python; sem migration. Verificado via Client: a tela renderiza o `<span data-notif-toggle>` (nao mais `<button>`). `docs/CONTEXTO.md` secao 5 atualizada.
- PERMISSOES (aba "Botoes do perfil" nao segurava a selecao de usuario): em "Personalizar um usuario", o select era um `<form method="get">` que submetia so `?user=<id>` e PERDIA o `?tab=botoes` — o reload caia na aba padrao (Perfis). CORRIGIDO: adicionado `<input type="hidden" name="tab" value="botoes">` no form do seletor, e os redirects nao-AJAX do POST `user` (salvar personalizacao) e `user-reset` (voltar ao padrao do perfil) passaram a redirecionar para `?tab=botoes&user=<id>` (antes iam para `?user=<id>` sem aba / para `permissions` puro). Agora selecionar/salvar/resetar mantem o admin na aba "Botoes do perfil" com o usuario ainda selecionado. Os toggles de personalizacao ja salvavam via AJAX (sem reload). So template + 2 redirects na view; sem migration. Verificado via Client. `docs/CONTEXTO.md` secao 15 atualizada.
- CONVERSAS (data nas mensagens e nas divisorias): antes cada balao e as divisorias de atendimento ("Atendimento encerrado" / "Novo atendimento iniciado") mostravam so a HORA (`%H:%M`). Agora mostram tambem a DATA. Backend: `_serialize_message` passou a expor `date` (`%d/%m/%Y`) alem de `time`. Frontend (`buildMessageEl`): cada balao mostra "DD/MM/AAAA HH:MM" discreto no rodape (`.conv-msg-time`, ja em 0.68rem cinza — sem mudanca de CSS) e a pilula da divisoria mostra "<texto> · DD/MM/AAAA HH:MM". So backend (1 campo) + JS inline no template; sem migration; sem CSS novo. Verificado via Client (mensagem: date=09/07/2026 time=18:37; divisoria: "Atendimento encerrado" 09/07/2026 18:44). `docs/CONTEXTO.md` secoes 5 e 12 atualizadas.
- CONVERSAS (separador entre data e hora): o carimbo de cada balao e a pilula da divisoria passaram a usar " · " entre a data e a hora — antes "14/07/2026 18:37", agora "14/07/2026 · 18:37" (balao) e "Atendimento encerrado · 14/07/2026 · 18:37" (divisoria). So JS inline no template (`buildMessageEl`); sem backend; sem migration; sem CSS.
- PERMISSOES (nova aba "Visualização de conversas"): modulo para configurar, POR SETOR e com EXCECAO POR USUARIO, o que cada atendente enxerga. (1) ALCANCE das conversas diretas — seletor de 4 niveis em `ConversationViewScope`: `own` (so as proprias), `sector_open` (abertas do setor + as dele = comportamento historico/padrao), `sector_all` (tudo do setor, inclui finalizadas de outros), `all` (todos os setores). (2) VER CONVERSA INTEIRA (`view_full_history`) — todo o historico do chat ou so o atendimento atual. Models: campos `Sector.view_scope`/`Sector.view_full_history` (padrao do setor) + novo `UserConversationView` (OneToOne, campos nulos = herdar do setor); migracao `0029`. Logica (`accounts/permissions.py`): `effective_view_scope` (usuario > mais permissivo entre os setores dele > padrao `sector_open`; `VIEW_SCOPE_RANK`) e `visible_conversations_q` reescrito por nivel; `history_full_for` agora le setor/usuario. GRUPOS continuam so pela aba Grupos (GroupAccess), independentes do alcance. O "Ver conversa inteira" SAIU da aba "Botoes do perfil" e centralizou aqui (as colunas `full_history` de RoleMenuPermission/UserMenuPermission ficaram legadas/sem uso). View: form_types `view-sectors`/`view-user`/`view-user-reset` (autosave AJAX; JS passou a salvar em `<select>` tambem). Template: nova aba + painel; removidos os toggles de full_history de Botoes. CSS `permissions.css?v=7`. Testes: `test_history_scope` migrado para setor; novos `test_history_full_user_override_beats_sector`, `test_scope_own_sees_only_assigned`, `test_scope_all_sees_other_sectors`, `test_scope_sector_all_sees_closed_of_sector`, `test_sector_scope_field_drives_visibility`, `test_effective_scope_most_permissive_multi_sector`, `test_view_tab_renders`, `test_save_sector_view`, `test_save_and_reset_user_view`. Suite 145 testes OK. Verificado via Client (render + salvar setor/usuario + reset). `docs/CONTEXTO.md` secoes 3 e 15 atualizadas.
- CONVERSAS (abas "Conversa do setor" x "Conversa privada"): ao abrir um contato (conversa DIRETA), se o historico visivel tiver atendimentos de mais de um dono, aparece uma barra de abas abaixo do cabecalho. "Conversa do setor" (padrao) mostra tudo o que a pessoa pode ver; "Conversa privada" mostra so os atendimentos que ela mesma atendeu. Backend: `conversation_messages_view` divide as mensagens em segmentos (entre as divisorias "Novo atendimento iniciado") e marca cada uma com `seg` e `seg_mine` (segmento com resposta minha — `sender_name`==meu nome de atendente — ou conversa atribuida a mim no segmento atual); retorna `owner_tabs` (so quando ha segmento meu E de outro; nunca em grupo). Front (`conversations.html`): barra `.conv-owner-tabs`, filtro visual `.conv-messages.filter-mine` que esconde `[data-seg-mine=0]`, `buildMessageEl` marca cada balao/divisoria; reseta para "Conversa do setor" ao trocar de conversa; filtro persiste no poll. CSS `conversations.css?v=25`. LIMITACAO: a separacao e por ATENDENTE (dono do atendimento), nao por setor historico — o banco so guarda o setor atual da conversa; seletor por setor exigiria gravar o setor por atendimento (proxima etapa, combinada com o usuario). Testes `test_owner_tabs_split_private_vs_sector` e `test_owner_tabs_hidden_when_single_owner`. `docs/CONTEXTO.md` secao 5 atualizada.
- CONVERSAS (seletor de setor por atendimento — evolucao das abas): agora, na aba "Conversa do setor", ha um seletor de setor (Todos os setores + um chip por setor presente no historico do contato) que filtra os atendimentos pelo SETOR. Base de dados: novo campo `Message.sector` (FK, nulo enquanto sem setor) carimbado NA CRIACAO com o setor atual da conversa, em todos os pontos (`save_incoming_message`, `save_outgoing_message`, `save_outgoing_media_message`, `save_system_message`, seed_demo_data); migracao `0030` com backfill do atendimento ATUAL de cada conversa (setor atual = confiavel; atendimentos antigos ficam sem setor, pois nao ha como saber o historico). Endpoint `conversation_messages_view`: resolve o `seg_sector` de cada atendimento como o ULTIMO setor nao-nulo do segmento (fallback para `Conversation.sector` no segmento atual) — assim o atendimento inteiro, incluindo a triagem sem setor e a divisoria, entra no setor onde terminou; retorna `conv_sectors` (setores presentes). Front: barra `.conv-filter-bar` (abas por dono + `.conv-sector-chips`), `applyFilters()` combina dono+setor com `.conv-msg-hidden` (substituiu o `.filter-mine`), reaplicado no poll e resetado ao trocar de conversa; a barra aparece quando ha >1 dono OU >1 setor. CSS `conversations.css?v=26`. Isso completa o pedido do seletor por setor (a limitacao anterior era so por atendente). Testes `test_segment_sector_resolution` e `test_new_message_stamps_sector`. `docs/CONTEXTO.md` secoes 3 e 5 atualizadas.
- CONVERSAS/PERMISSOES (ajustes de UX pedidos): (1) as abas "Conversa do setor" x "Conversa privada" ficavam escondidas quando o contato tinha atendimentos de um so dono — agora aparecem sempre que ha MAIS DE UM atendimento no historico visivel (`owner_tabs = len(segmentos) >= 2`), ficando descobriveis (lembrando que dependem de "ver conversa inteira" para enxergar atendimentos anteriores). (2) Em Permissoes > Visualizacao de conversas, o "Alcance" deixou de ser lista suspensa e virou um SLIDER visual (barra "menos -> mais visualizacao", 4 niveis own/sector_open/sector_all/all) com preenchimento em gradiente (azul->verde), thumb com brilho/escala e rotulo do nivel embaixo; por usuario ha um checkbox "Herdar do setor" que desabilita/apaga o slider. O contrato de backend nao mudou (o slider grava o mesmo `<input hidden>` com o valor do escopo; niveis vem via `json_script` `scope-levels`). CSS `permissions.css?v=8`; `conversations.css` inalterado nesta etapa. Testes existentes seguem validos (POST direto do escopo). `docs/CONTEXTO.md` secoes 5 e 15 atualizadas.
- CONVERSAS/CONTATOS (nome so quando cadastrado): a conversa direta passou a aparecer com o NUMERO por padrao, e o nome so aparece depois que alguem CADASTRA o contato clicando no numero (o cadastro grava o Contact e por isso ja aparece na tela Contatos). Antes, a 1a mensagem de uma conversa direta criava o Contato ja batizado com o pushName do WhatsApp (`get_or_create_contact(phone, ctx['sender_name'])`), entao nunca aparecia numero para clicar. Agora `get_or_create_contact(phone)` cria o contato SEM nome (`name=''`) e nao usa mais o pushName; `Contact.display_name` cai para o numero e nome cadastrado a mao nunca e sobrescrito. Mesma regra no GRUPO: `_build_name_map` resolve o remetente/mencoes SO por Contato cadastrado (o pushName saiu da resolucao), entao o participante aparece como numero clicavel ate ser cadastrado — o front ja tratava esse caso (`conv-msg-sender-num`). EXCECAO: conversa direta sem telefone (`@lid`) mantem o pushName como titulo, porque nao ha numero para clicar/cadastrar (o alternativo seria um id interno impossivel de nomear). FRONT (cabecalho da direta): `_serialize_contact_info` passou a expor `contact_name` (nome REALMENTE cadastrado; vazio = falta cadastrar) alem de `name` (que cai para o numero) — sem isso o modal abria com o telefone escrito no campo de nome; sem nome cadastrado o numero ganha destaque pontilhado visivel (`.conv-chat-name-unnamed`), o tooltip vira "Clique no numero para cadastrar este contato" e a linha de baixo mostra "Sem nome cadastrado — clique no numero" (em vez de repetir o numero); ao salvar, cabecalho/telefone/painel de info atualizam na hora. `conversations.css?v=27`. Novo comando `cleanup_pushname_contacts [--apply]` (dry-run por padrao) para os contatos JA criados com pushName: limpa o nome apenas quando ele e identico (ignorando caixa/espacos) ao `sender_name` de alguma mensagem RECEBIDA daquele numero, isto e, ao pushName do historico — nome digitado a mao nao bate com nenhum pushName e e PRESERVADO. Testes `ContactDefaultsToNumberTests` (contato nasce sem nome/exibe numero, pushName nao sobrescreve nome cadastrado, endpoint expoe `contact_name` vazio, cadastrar pelo numero mostra o nome e cai em Contatos, grupo mostra numero ate cadastrar) e `CleanupPushnameContactsCommandTests` (dry-run nao altera, --apply limpa pushName e preserva nome manual, ignora caixa/espacos, nada a fazer). Sem migration. `docs/CONTEXTO.md` secoes 3, 5 e 9 atualizadas.
- CONVERSAS/@lid (telefone real da conversa direta): descoberto no VPS que a instancia W-API Lite entrega TODAS as conversas diretas chaveadas por um id interno (`@lid`) — em producao eram 9 diretas, todas `@lid`, e ZERO com telefone. Nesse caminho o sistema nao criava `Contact` nenhum, entao a etapa anterior (nome so quando cadastrado) ficava dormente: o titulo vinha de `Conversation.name` = pushName e nao havia numero para clicar. CORRIGIDO usando o proprio payload, sem nenhuma chamada nova a W-API: o telefone REAL ja vem no remetente (`sender.id`, ex.: chat `53094503153686@lid` + sender `5519971548270`) e o parser ja o extraia para `Message.sender_id`. Parser (`wapi/parser.py`): `normalize_wapi_message_context` passou a expor `sender_phone` (telefone valido de quem enviou; vazio se o remetente for so id interno) e `connected_phone` (o NOSSO numero, via novo `_CONNECTED_PHONE_PATHS`). Servico (`wapi/services.py`): nova `attach_contact_from_sender(conversation, ctx)`, chamada no branch `@lid` de `resolve_conversation_for_context` (na criacao E em toda mensagem seguinte), anexa o `Contact` do telefone real — a conversa passa a aparecer pelo NUMERO (clicavel para cadastrar) e a pessoa fica UNIFICADA com os grupos e a tela Contatos (mesmo telefone = mesmo Contato; nos grupos `sender.id` tambem e o telefone, o `senderLid` e que e o LID). A conversa CONTINUA chaveada pelo `@lid` (`external_id`), que e o destino exigido pela W-API no envio — nenhum chat e dividido nem unido, e `display_title` ignora o `name` quando ha contato (o pushName fica so como ultimo recurso de titulo). Guardas: nao anexa se ja tem contato, se e grupo, se a mensagem e NOSSA (`from_me`, quando `sender.id` e o numero da instancia), se o remetente nao e telefone valido, ou se o numero e o `connected_phone`. Novo comando `link_lid_contacts [--apply]` (dry-run por padrao) para as conversas `@lid` que ja existiam sem contato: resolve o telefone pelo remetente mais frequente das mensagens RECEBIDAS (nunca o `connectedPhone`), anexa o Contato e reaproveita contato ja nomeado em grupo (ai a direta ja aparece com o nome); conversa sem mensagem recebida fica como esta (resolve sozinha na proxima). Testes `LidRealPhoneTests` (parser expoe telefone/nosso numero, sem telefone quando o remetente e LID, mensagem `@lid` vincula o contato do telefone real, cadastrar o numero nomeia a conversa, contato compartilhado com o grupo, `fromMe` nao vira contato, `connectedPhone` nunca vira contato, participante de grupo nao gera contato privado, conversa antiga se resolve na proxima mensagem) e `LinkLidContactsCommandTests` (dry-run, --apply, reaproveita contato nomeado, conversa sem historico, nada a fazer). Sem migration. `docs/CONTEXTO.md` secoes 3, 4 e 9 atualizadas.
- WEBHOOK (payload descartado criava CONVERSA VAZIA): corrigida a ORDEM das operacoes em `ingest_wapi_payload` (`wapi/services.py`). A conversa era resolvida/criada ANTES dos descartes por tipo nao suportado (`message_type == 'unknown'`) e por texto vazio, entao todo payload descartado deixava uma conversa SEM NENHUMA MENSAGEM para tras — que aparecia na lista com o JID/@lid cru no titulo. Em producao eram 5 conversas fantasma: 2 diretas de `templateMessage` (propaganda de empresa, ex.: oferta do PicPay) e 3 de grupo (`protocolMessage` do sistema e eventos `action`+`participants` de entrada/saida de participantes). Pior: para um GRUPO novo o `resolve_conversation_for_context` ainda chamava `resolve_group_name`, gastando uma requisicao a W-API para batizar um grupo que ficava vazio. Agora o conteudo (tipo, texto/legenda e metadados de midia) e resolvido PRIMEIRO e a conversa so e criada quando ha algo para salvar; os tres ramos de save (texto/reacao/midia) foram unificados numa unica chamada a `save_incoming_message` (o helper `_finish` saiu, o disparo de `_maybe_trigger_reception` continua igual e ja tratava `message=None` da deduplicacao). Ganho extra: uma conversa ENCERRADA nao reabre mais (`_reopen_for_new_service`) por causa de um evento de sistema. Adicionado log `ignorado (texto vazio)` e o comentario do descarte passou a citar `templateMessage`. Testes `DiscardedPayloadLeavesNoConversationTests` (templateMessage/sistema de grupo/texto vazio nao criam conversa nem contato, `resolve_group_name` nao e chamado, mensagem real e midia continuam chegando com metadados, evento de sistema nao reabre conversa fechada). As 5 conversas vazias que JA existiam nao foram removidas (decisao do usuario: so a correcao). Sem migration. `docs/CONTEXTO.md` secao 4 atualizada.
- MULTIEMPRESA (SaaS) — PARTE 1 (fundação): o BEEZAP passou a atender VÁRIAS EMPRESAS CLIENTES na mesma instalação. Novo model `Company` (nome fantasia, razão social, CNPJ, identificador, e-mail, telefone, endereço, cidade/UF, logo, cor de destaque, observações, ativa) e novo perfil `master` (gestor da plataforma) em `User.role`, com `User.company` (nulo = master, que fica acima das empresas). Campo `company` OBRIGATÓRIO em Attendant, Sector, Contact, Conversation, WapiWebhookEvent, RoleMenuPermission e nas três configurações; Message/MenuOption/GroupAccess/UserMenuPermission/UserConversationView/PasswordResetCode derivam a empresa (sem campo próprio). As unicidades GLOBAIS de `Sector.name`, `Contact.phone` e `RoleMenuPermission.role` viraram unicidades POR EMPRESA (duas empresas podem ter o setor "Financeiro" e o mesmo telefone de cliente). `WapiConfiguration`/`OpenAiConfiguration`/`MenuBotConfiguration` deixaram de ser singleton `pk=1`: agora é uma por empresa via `for_company(company)` (`get_solo()` ficou como compatibilidade apontando para a empresa padrão), e `record_usage`/`record_last_exchange` viraram métodos de instância — o consumo de tokens passa a ser contado no cliente certo. Novo `accounts/tenancy.py` (is_master, current_company, scoped, require_master, deny_master_json) e `accounts/context_processors.py` (marca do cliente em todas as telas). Nova tela CLIENTES (`clientes/`, `clients_view`, só master) com resumo, busca, cartões mostrando logo/dados/selos e os contadores reais de usuários e conversas, e modal de cadastro/edição (`CompanyForm`: logo PNG/JPG/WEBP/SVG até 2 MB, CNPJ de 14 dígitos guardado só em dígitos, telefone com DDD, UF em maiúscula, identificador gerado pelo nome) — a empresa padrão não pode ser excluída nem desativada, bloqueado também no backend. A barra lateral, que estava COPIADA em 8 templates, virou o include `accounts/_sidebar.html` e passou a mostrar o logo e o nome DA EMPRESA de quem está logado (sem logo, as iniciais na cor de destaque dela); o master vê a marca do BEEZap com "Gestão de clientes". O master só vê o botão Clientes, recebe 403 nas telas operacionais e NÃO enxerga conversa nenhuma (decisão de privacidade/LGPD); `visible_conversations` passou a filtrar pela empresa antes do Alcance, então o admin agora vê tudo DA EMPRESA dele. Migração `0031_multiempresa` em 4 fases (cria Company → adiciona company nulo → cria a "Empresa padrão" e aponta TODOS os registros existentes para ela → torna obrigatório e troca as unicidades), então nada muda para quem já usa. `clients.css?v=1` (responsivo: cartão em coluna única abaixo de 900px, formulário em uma coluna abaixo de 620px), `dashboard.css?v=5`. Testes: 207 passando (29 novos cobrindo isolamento por empresa, perfil master, tela Clientes e a marca do cliente). ATENÇÃO: o isolamento das consultas ainda é da Parte 2 — não cadastrar um segundo cliente para uso real antes dela (checklist no final da seção 16 do CONTEXTO.md).
- MULTIEMPRESA (SaaS) — PARTE 2a (motor por empresa): cada empresa passou a usar a SUA instancia da W-API e a SUA API Key do GPT. Em `wapi/client.py` todas as funcoes publicas (`send_text_message`, `send_image_message`, `send_audio_message`, `send_video_message`, `send_document_message`, `download_media`, `get_all_groups`) passaram a exigir `company` SOMENTE-NOMEADO, e `_company_config` levanta erro se vier None — deixar a empresa implicita mandaria a mensagem pelo WhatsApp de outro cliente. Novo webhook POR CLIENTE: rota `webhook/wapi/<empresa>/` (`wapi-webhook-company`), com a rota antiga mantida; `resolve_webhook_company` identifica a empresa em tres degraus (identificador da URL -> `instanceId` do payload -> empresa padrao), empresa inativa/desconhecida devolve 404 sem criar nada, e o token de webhook passou a ser validado POR EMPRESA (depois de identificar o cliente). A IA foi escopada: `available_sectors(company)`, `available_attendants(company)`, `build_system_prompt(config, company, ...)`, `_match_sector/_match_attendant(name, company)` e `_resolve_fallback_sector(config, company)` — isso era um VAZAMENTO REAL, porque sem o escopo a IA listava no prompt (e oferecia ao cliente final) os setores e atendentes de outra empresa. `gpt/client.chat_completion`/`test_connection` recebem `company` (API Key, modelo e contador de tokens da empresa) e o chatbot de menu le textos/opcoes/modo/fallback de `MenuBotConfiguration.for_company(conversation.company)`. As telas WhatsApp/IA/Atendimento e o seletor de modo passaram a ler e gravar a config da empresa de quem esta logado, a URL de webhook exibida e a do cliente e os eventos listados sao so dele; os selects de setor de fallback (IA e chatbot) e a validacao de nome de setor passaram a considerar apenas a propria empresa. Comandos: `sync_wapi_group_names` roda por empresa (`--empresa`, ou todas as ativas), `inspect_wapi_groups` aceita `--empresa`, `sync_wapi_events_to_conversations` usa a empresa do proprio evento e `link_lid_contacts` resolve o contato na empresa da conversa; a recuperacao de senha envia pelo WhatsApp da empresa da pessoa. Nenhum `get_solo()` restou no codigo de producao. Sem migration.
- MULTIEMPRESA (SaaS) — PARTE 2b (telas escopadas): antes, as listas consultavam o banco sem filtro de empresa, entao com dois clientes cadastrados um veria os contatos, setores, atendentes, grupos e indicadores do outro. Passaram a filtrar por `request_company(request)`: Contatos (lista, contagem, busca, edicao e exclusao), Setores (lista, edicao, exclusao e o arrastar-e-soltar, incluindo a re-inclusao automatica dos admins), Atendentes (lista e edicao — id de outro cliente da 404), Permissoes (pessoas, padroes por perfil, personalizacao por usuario, setores da aba Visualizacao e grupos) e Dashboard (todos os indicadores). A transferencia passou a oferecer e aceitar somente setor/atendente da MESMA empresa da conversa, entao um id forjado de outro cliente nao tem efeito; a resolucao de nome nas mensagens de grupo (remetente e mencoes @) usa apenas contatos da propria empresa; e a regra "deve existir ao menos um administrador" passou a valer POR EMPRESA. Sem migration.
- MULTIEMPRESA (SaaS) — PARTE 2c (acesso do cliente e modo suporte): o gestor master passou a poder colocar um cliente no ar de ponta a ponta. Na tela CLIENTES, cada empresa sem acesso mostra "Criar o acesso do cliente" (`action=create-admin`, novo `CompanyAdminForm`: nome, e-mail, senha inicial e WhatsApp) — cria o ADMINISTRADOR daquela empresa (`role=adm`, `company=<empresa>`), provisiona o `Attendant` dele com `must_change_password=True` (o `InitialPasswordChangeMiddleware` obriga a troca no primeiro acesso) e garante o setor Geral da empresa nova; o e-mail continua único no sistema inteiro, porque é a chave de login. Empresas que já têm acesso mostram os administradores no cartão. Novo MODO SUPORTE: "Entrar no painel" (`action=enter`) grava a empresa na sessão via `tenancy.set_active_company` e leva o master para a tela WhatsApp/W-API daquele cliente; nesse modo ele acessa APENAS as telas de configuração (`MASTER_SUPPORT_KEYS = settings/sectors/attendants/permissions`) — Conversas, Contatos e Dashboard continuam bloqueados de propósito (dados pessoais dos clientes finais), e `visible_conversations` segue devolvendo vazio para o master mesmo dentro do painel. `user_can_access` e `nav_items_for` receberam o parâmetro `in_company`, calculado por `master_in_company(request)`; `build_nav_items` passou a receber o `request`. A barra lateral e a tela Clientes mostram um aviso âmbar "Modo suporte — você está no painel de X" com o botão "Sair do painel" (`action=leave`), para o master nunca confundir de quem é o painel. Correção de dois bugs achados pelos testes: `resolve_webhook_company` não estava importado em `accounts/views.py` (o webhook daria erro 500) e o envio de texto da conversa e a lista de eventos do webhook ainda não passavam a empresa. `clients.css?v=2` e `dashboard.css?v=5` (aviso do modo suporte; cartão de cliente com 4 colunas que colapsam em 1100px e 900px, formulário de acesso recolhido em `<details>` para não poluir). Testes: 247 passando (40 novos na Parte 2, cobrindo roteamento do webhook por empresa/instanceId/empresa inativa, token por empresa, o mesmo número falando com duas empresas, envio pela instância certa, isolamento de contatos/setores/atendentes/permissões/conversas/dashboard/transferência, criação do acesso do cliente e o modo suporte). Com isso o multiempresa está COMPLETO: já é possível atender vários clientes de verdade ao mesmo tempo, cada um com o seu WhatsApp, a sua IA e os seus dados. Próxima etapa: Parte 3 (exportar os dados da empresa).
- MULTIEMPRESA — SEPARACAO TECNICO x NEGOCIO: o que envolve CREDENCIAL e CUSTO passou a ser exclusivo do gestor master; o cliente configura so o que e conteudo do negocio dele. (1) GPT VOLTOU A SER UMA CONFIGURACAO UNICA DA PLATAFORMA (migration `0032_openai_config_da_plataforma`): `OpenAiConfiguration` perdeu o vinculo `company` (que a Parte 1 tinha criado) e o campo `fallback_sector`; a migration junta as linhas por empresa numa so, preferindo a que ja tem API Key cadastrada, e `get_solo()` passou a buscar a primeira linha por id (nao `pk=1` fixo). Assim o master cadastra UMA API Key e ela atende todos os clientes; `gpt/client.chat_completion`/`test_connection` deixaram de receber `company`. O `fallback_sector` da IA saiu de cena porque setor pertence a uma empresa: `_resolve_fallback_sector(company)` passou a usar o `fallback_sector` do CHATBOT daquela empresa e, na falta dele, o setor Geral dela — um conceito so ("para onde mandar quando nao entender"), e a conversa nunca fica sem fila. O `enabled` (vestigial) passou a refletir se ALGUMA empresa ativa esta usando a IA, em vez de ser desligado quando um unico cliente sai da IA. (2) SO O WHATSAPP CONTINUA POR EMPRESA (instancia + token da W-API de cada cliente) e a tela ficou EXCLUSIVA DO MASTER, acessivel apenas dentro do painel do cliente (`require_master_in_company`: 403 para perfil de cliente e redirect amigavel para Clientes quando o master ainda nao escolheu a empresa). (3) A tela INTELIGENCIA (IA) saiu da area Configuracoes do cliente e virou uma tela da PLATAFORMA no menu do master, ao lado de Clientes (`MASTER_ONLY_ITEMS`), com `require_master` e um aviso explicando que a chave vale para todas as empresas + quantas estao com a IA ligada. (4) O CLIENTE ficou com a area Atendimento: edita o chatbot de menu (saudacao, opcoes, mensagens, tentativas, fallback) e ESCOLHE QUALQUER MODO de primeiro atendimento (desligado / chatbot / IA) — o botao Configuracoes dele passou a apontar para `atendimento` em vez de `wapi-settings`, e a barra de abas so mostra a aba WhatsApp quando quem esta olhando e o master. (5) Novo CARD DE STATUS na tela do cliente (`build_service_status`): "WhatsApp conectado" / "ainda nao configurado" e "Inteligencia (IA) disponivel" / "indisponivel", com orientacao de falar com o administrador da plataforma — sem nunca exibir Instance ID, token ou API Key (ha teste garantindo). Verde = pronto, ambar = pendente. Dois bugs achados pelos testes e corrigidos: `atendimento_set_mode_view` ainda chamava `OpenAiConfiguration.for_company` (trocar o modo quebrava com AttributeError) e o retorno do seletor de modo ainda apontava para a sub-aba de IA, que nao existe mais. `settings_tabs.css?v=2` (card de status), `openai_settings.css?v=7` (aviso de configuracao da plataforma). Testes: 266 passando (18 novos cobrindo o bloqueio das telas tecnicas para o cliente inclusive por POST forjado, o cliente salvando chatbot e trocando de modo, o isolamento do modo entre empresas, o status sem vazar credencial, o master salvando a chave unica e o fallback da IA vindo do chatbot da propria empresa).
- Docs: regra de versionamento reforcada no `CONTEXTO.md`. A secao 8 (Fluxo de trabalho obrigatorio) ganhou um aviso em destaque de que NENHUMA alteracao fica sem `commit` + `push` — toda mudanca (codigo, CSS, template ou documentacao) e fechada no mesmo passo, sem acumular alteracoes locais para "commitar depois", porque o repositorio remoto e sempre o estado real do projeto; se o `python manage.py check` falhar, corrigir antes (nao se commita com o check quebrado). Os passos tambem passaram a citar que a documentacao afetada e atualizada junto com o `HISTORICO.md`, que a mensagem de commit e em PT-BR e que o `push` e imediato, com codigo + docs no mesmo commit. A regra ja existia em `GIT.md` e `CODEX_PADROES.md`; agora esta tambem no documento de handoff, que e o primeiro lido em cada sessao nova. Sem alteracao de codigo e sem migration.
- Docs: correcao de referencias desatualizadas no `CONTEXTO.md`, conferidas contra o codigo. A secao 3 dizia "migracao atual: 0031" quando a ultima ja e a `0032_openai_config_da_plataforma`; e as citacoes de cache-busting de CSS estavam velhas — `conversations.css` aparecia como `?v=20` (secao 6), `?v=21` (secao 12) e `?v=23` (secao 15), mas o template ja esta em `?v=27`, e `openai_settings.css` aparecia como `?v=3` na secao 13 quando o real e `?v=7`. Todas atualizadas. Conferido tambem que continua batendo: os 15 comandos de management listados, os modulos `wapi/`/`gpt/`/`chatbot/`, os 14 templates e a suite de testes (266 testes, OK). Sem alteracao de codigo e sem migration.
- SEGURANCA (Etapa A) — MIDIA DAS CONVERSAS DEIXOU DE SER PUBLICA. Furo real encontrado numa auditoria do isolamento: os arquivos recebidos eram salvos como `media/whatsapp/wapi_<id_da_mensagem>.<ext>` (nome SEQUENCIAL) e o Nginx servia `/beezap/media/` inteiro, sem autenticacao — qualquer pessoa que trocasse o numero na URL baixava foto, audio, video ou documento de QUALQUER empresa, inclusive sem estar logada. Agora o arquivo so sai por duas rotas do Django: `midia/<id>/` (`message-media`, exige login e passa por `can_see_conversation`, entao vale empresa + alcance — e o gestor master tambem recebe 403, inclusive no modo suporte) e `midia-publica/<token>/` (`media-public`, link ASSINADO com `django.core.signing`, valido para UMA mensagem e por 15 min, que existe so porque a W-API roda na nuvem e baixa pela URL a midia que enviamos). `Message.resolved_media_url` passou a devolver a rota autenticada em vez de `media_file.url`, e `conversation_send_media_view` passou a gerar o link assinado (o envio local continua caindo em base64). Os arquivos recebidos passaram a ser salvos com nome UUID. No Nginx, `location /beezap/media/` deve ser trocado por `location /beezap/media/empresas/` (so os logos) — DEPLOY.md ganhou a secao com o passo e o `curl` de conferencia; sem esse ajuste no VPS o caminho antigo continua aberto. Tambem: a aba GRUPOS da tela Permissoes (que lista os NOMES dos grupos de WhatsApp do cliente) deixou de aparecer para o master, e os POSTs `groups`/`group-name`/`group-remove` devolvem 403 para ele — esconder a aba nao bastaria. `nginx_beezap.conf.example` atualizado nas duas opcoes (dominio proprio e prefixo /beezap/). Testes: 14 novos (dono abre o arquivo; outra empresa 403; master 403; master no modo suporte 403; anonimo vai para o login; serializer aponta para a rota protegida; nome de arquivo nao adivinhavel; token valido/invalido/expirado; ADM ainda ve a aba Grupos; master nao ve nome de grupo nem renomeia/remove por POST forjado). Sem migration.
- MULTIEMPRESA (Etapa B) — TELA DE METRICAS DO CLIENTE para o gestor master, em `clientes/<id>/metricas/` (`client_metrics_view`, so master; o ADM do cliente recebe 403), com o botao "Metricas" no cartao de cada empresa na tela Clientes. Responde "qual o tamanho deste cliente e ele esta usando o sistema?" sem furar a regra de que o master nao le o atendimento: a tela e SO numeros, datas e estado do canal. `build_company_metrics(company)` reune CANAL (credenciais configuradas sim/nao — nunca o Instance ID ou o token —, modo de primeiro atendimento, ultimo evento de webhook e total de eventos), MENSAGENS (enviadas e recebidas no total/7/30 dias, respostas automaticas da IA ou do chatbot, mensagens com arquivo e a data-hora da ultima enviada e da ultima recebida), CONVERSAS (ativas, aguardando, finalizadas, grupos, novas em 7 dias) e EQUIPE (atendentes, administradores, usuarios ativos, setores, contatos); as divisorias de atendimento (`message_type=system`) ficam fora da contagem. SAUDE DA CONEXAO: novo `wapi.client.check_connection(company=...)` com o dataclass `WapiHealth`, que consulta `status-instance` (novo `WAPI_INSTANCE_PREFIX`) e, se essa rota nao existir no plano da conta (404/405), sonda com `get-all-groups`; le o estado por varios nomes conhecidos de campo (`connected`/`isConnected`/`status`/...), reporta credencial recusada em 401/403 e nunca levanta excecao nem expoe token. Fica atras do botao "Testar conexao" (endpoint `client-connection-check`, POST, so master) de proposito: verificar na abertura faria uma chamada externa por empresa a cada visita. Novos `templates/accounts/client_metrics.html` e `static/css/client_metrics.css?v=1` (escopo `.metrics-page`, cartoes iguais, aviso de privacidade no topo, responsivo em coluna unica abaixo de 640px). Testes: 15 novos, incluindo os que garantem que a tela NAO mostra texto de mensagem, nome de contato, nome de grupo, telefone nem credencial, que os numeros contam so a empresa daquela tela e que o ADM do cliente nao entra. Sem migration.
- MULTIEMPRESA — PARTE 3 (PORTABILIDADE) CONCLUIDA, com uma mudanca de dono em relacao ao que estava planejado: QUEM EXPORTA E O CLIENTE, NAO O MASTER. A documentacao original previa o gestor master baixando o ZIP dos dados da empresa; isso foi revisto porque contraria a regra de que ele administra sem ler o atendimento — um ZIP com todas as conversas seria justamente ler tudo de uma vez. Nova aba MEUS DADOS em Configuracoes (`configuracoes/dados/`, `company_data_view`, rota `company-data`; download em `company-export`), ao lado de Atendimento, para o ADM da empresa: explica o que vem no arquivo, mostra o resumo (contatos, conversas, mensagens e arquivos) e faz o download. Bloqueios no BACKEND, nao so a aba escondida: `require_feature(settings)`, `_deny_master_export` (403 para o master, INCLUSIVE no modo suporte) e `block_readonly` (o perfil leitor nao exporta); a empresa vem de quem esta logado e o endpoint NAO aceita id de empresa, entao nao existe exportar a do vizinho por URL forjada. Novo modulo `accounts/export.py` monta o ZIP com `LEIA-ME.txt`, `empresa.json`, `setores.csv`, `atendentes.csv`, `usuarios.csv` (SEM senha nem hash), `contatos.csv`, `conversas.csv`, `mensagens.csv` e a pasta `midias/`; CSV em UTF-8 com BOM e separador ";" (abre direto no Excel pt-BR), `mensagens.csv` liga em `conversas.csv` pela coluna `conversa_id` e a coluna `arquivo` aponta para o arquivo dentro do ZIP, renomeado para `<id_da_mensagem>-<nome_original>` (em disco o nome e um uuid, que nao diria nada a quem abre o pacote). Escala: o ZIP e escrito num arquivo TEMPORARIO EM DISCO (nao em memoria) e as mensagens sao percorridas com `iterator(chunk_size=500)`, para anos de historico nao derrubarem o gunicorn; midia que sumiu do disco e pulada e nunca derruba a exportacao inteira. ENCERRAR CLIENTE COM SEGURANCA na tela Clientes: a exclusao passou a exigir, no backend, que a empresa esteja DESATIVADA (o atendimento para primeiro e o cliente tem tempo de exportar) E que o master digite o NOME EXATO da empresa (`confirm_name`, comparado sem diferenciar maiusculas); o `confirm()` simples do template virou um prompt que pede o nome. A empresa padrao continua inexcluivel. Ordem recomendada: cliente exporta -> desativar -> excluir. A exclusao tambem passou a APAGAR OS ARQUIVOS DE MIDIA DO DISCO (`_delete_company_media_files`): o `delete()` em cascata limpa o banco, mas o Django nao remove o arquivo, entao as fotos e documentos do cliente ficariam orfaos no servidor para sempre, sem ninguem conseguir ver nem apagar pela interface; a mensagem de sucesso informa quantos arquivos sairam. Novos `templates/accounts/company_data.html` e `static/css/company_data.css?v=1`; `_settings_tabs.html` passou a mostrar as abas do CLIENTE ([Atendimento] [Meus dados]) — antes so o master via abas. Testes: 17 novos (ADM abre e baixa; master 403 na pagina e no download mesmo no modo suporte; leitor 403; anonimo vai para o login; o ZIP tem todos os arquivos, o historico e as midias; NUNCA tem dado de outra empresa nem senha; cada empresa exporta so a sua; midia faltando no disco nao quebra; exclusao barrada com empresa ativa, barrada com nome errado, executada com empresa desativada + nome certo, e empresa padrao nunca excluida). Sem migration.
- Docs: `README.md` passou a linkar toda a documentacao oficial. Ele listava so INDEX, CODEX_PADROES, GIT e HISTORICO — faltavam justamente o `CONTEXTO.md` (que o proprio INDEX define como "comece por aqui", o documento de handoff lido no inicio de cada sessao), o `DEPLOY.md` e o `WAPI_MEDIA_INTEGRATION.md`. Quem chegava pelo GitHub nao encontrava o handoff pela porta da frente. Cada link ganhou uma linha curta dizendo para que serve o arquivo, e o CONTEXTO ficou em primeiro, em destaque. Conferido na mesma passagem que a documentacao continua batendo com o codigo (migracao `0032`, `conversations.css?v=27`, `dashboard.css?v=5`, `settings_tabs.css?v=2`, `openai_settings.css?v=7`, os 15 comandos de management e os modulos `wapi/`/`gpt/`/`chatbot/`) e com o VPS: o servidor ja responde nas rotas novas (`configuracoes/dados/`, `configuracoes/dados/exportar/`, `clientes/<id>/metricas/`, `midia/<id>/`) e o Nginx ja esta com o ajuste de midia aplicado (`/beezap/media/whatsapp/...` devolve 404 e so `/beezap/media/empresas/` continua servido). Sem alteracao de codigo e sem migration.
- MODO SUPORTE ENXUTO — o master no painel do cliente ve so o WHATSAPP. Antes, ao "Entrar no painel", ele ganhava Configuracoes (com as abas WhatsApp e Atendimento), Setores, Atendentes e Permissoes daquele cliente (`MASTER_SUPPORT_KEYS = {'settings','sectors','attendants','permissions'}`). Decisao do usuario: dentro do painel devem aparecer apenas os botoes da PLATAFORMA (Clientes e Inteligencia (IA), que vale para todas as empresas) e o WHATSAPP, que e onde se configura a instancia/token da W-API — a unica parte TECNICA, com credencial, que nao pode ficar na mao do cliente. Tudo o que e NEGOCIO da empresa (setores, atendentes, permissoes e o Atendimento: chatbot de menu + modo de primeiro atendimento) passou a ser exclusivo do ADM dela. Em `accounts/permissions.py`, `MASTER_SUPPORT_KEYS` deu lugar a `WHATSAPP_ITEM` ({'label': 'WhatsApp', 'url_name': 'wapi-settings'}): `user_can_access` devolve False para o master em QUALQUER key de `MENU_FEATURES` (com ou sem painel aberto) e perdeu o parametro `in_company`, que deixou de mudar o que ele pode; `nav_items_for` monta o menu do master como MASTER_ONLY_ITEMS + (no modo suporte) o botao WhatsApp, e nao acrescenta mais o PERMISSIONS_ITEM. Nao e so o botao escondido: `sectors`, `attendants`, `permissions` e `atendimento` passam por `require_feature`, entao agora respondem 403 para o master inclusive por POST forjado (a tela do WhatsApp continua protegida por `require_master_in_company`, que exige ser master E estar dentro do painel — ela nunca dependeu da matriz de features). `_settings_tabs.html` mostra so a aba WhatsApp para o master (a aba Atendimento levaria a um 403) e os textos da tela Clientes que prometiam "ajustar WhatsApp, IA, setores, atendentes e permissoes" e "e ele quem cria os atendentes, os setores e as configuracoes do WhatsApp" foram corrigidos para a nova divisao. CONSEQUENCIA OPERACIONAL: o master nao monta mais a operacao do cliente — o passo-a-passo de colocar cliente novo no ar virou cadastrar empresa -> criar o acesso do ADM -> colar instancia/token no painel -> sair, e setores/atendentes/permissoes/chatbot ficam para o Administrador do cliente no primeiro acesso (secao 16 do CONTEXTO.md atualizada, junto com o menu do perfil master, o quadro "O que o master NAO alcanca" e o bloco do modo suporte). A checagem `show_groups_tab` em `permissions_view` ficou como segunda barreira (o master ja nem chega na tela) e o comentario dela diz isso. Testes: `MasterSupportModeTests` reescrito — o master abre WhatsApp e a IA da plataforma (200), leva 403 em setores/atendentes/permissoes/atendimento, POST forjado nao cria setor para o cliente e o menu no painel e exatamente ['Clientes', 'Inteligencia (IA)', 'WhatsApp']; `MasterDoesNotSeeGroupNamesTests` passou a esperar 403 na tela inteira de Permissoes. Suite: 314 testes passando. Sem migration e sem mudanca de CSS.
- GESTOR MASTER: TELA GESTORES (master cria master) + RECUPERACAO DE SENHA PARA ELE. Ate aqui o master so nascia pelo shell do VPS (`create_user(role=MASTER)`) e, se perdesse a senha, so o shell resolvia — a plataforma nao tinha sucessor, substituto de ferias nem caminho de recuperacao para o proprio dono. Isso ficou evidente na pratica: seguindo o exemplo da documentacao ao pe da letra criou-se em producao um master `master@seudominio.com` com a senha `TROQUE-ESTA-SENHA`, que esta escrita no repositorio publico. (1) NOVA TELA GESTORES (`gestores/`, `masters_view`, rota `masters`, `MASTERS_ITEM` no menu do master ao lado de Clientes e Inteligencia (IA)), exclusiva do master via `require_master` — perfil de cliente leva 403 na pagina E no POST. "Novo gestor" (`action=create`, novo `MasterUserForm`): nome, e-mail, senha inicial de no minimo 8 caracteres e WHATSAPP OBRIGATORIO; cria `role=master`, `company=None`, `recovery_phone` preenchido e `must_change_password=True`. Por cartao: `save-phone` (troca o WhatsApp de recuperacao), `reset-password` (nova senha inicial que volta a exigir troca — senha definida por outra pessoa e sempre provisoria), `toggle-active` e `delete`. TRAVAS NO BACKEND, nao so na tela: ninguem desativa nem exclui a PROPRIA conta (para nao se trancar fora), NUNCA sobra zero master ativo (`_active_masters_besides`, senao a plataforma fica sem dono e sem quem cadastre cliente ou credencial) e excluir exige DESATIVAR ANTES, a mesma ordem ja adotada na tela Clientes. (2) RECUPERACAO DE SENHA DO MASTER: o envio ja sabia lidar com quem nao tem empresa (`create_and_send_password_recovery_code` usa a instancia da EMPRESA PADRAO quando `user.company` e nulo), mas faltava o TELEFONE — `get_user_recovery_phone` lia so `user.attendant_profile.phone` e o master nunca teve `Attendant`, entao ele caia no `return ''` e nenhum codigo era enviado. Migration `0033_gestor_master_recuperacao` adiciona dois campos ao `User` para quem nao tem atendente: `recovery_phone` e `must_change_password`. `get_user_recovery_phone` passou a cair no `recovery_phone`, o `InitialPasswordChangeMiddleware` passou a olhar OS DOIS lugares (User e Attendant) e `change_initial_password_view` deixou de exigir `attendant_profile` (o master era redirecionado para fora da tela de troca antes de conseguir trocar) e limpa a marca nos dois. Novos `templates/accounts/masters.html` e `static/css/masters.css?v=1` (escopo `.masters-page`/`.masters-modal`, cartoes em grade que viram coluna unica abaixo de 900px, selo ambar "Troca a senha no proximo acesso", ferramentas de acesso recolhidas em `<details>` para nao poluir). Documentacao: secao 3 (campos novos e migracao `0033`), o item do `PasswordResetCode`, o menu do master na secao 16 e a secao "Como criar o gestor master", que passou a dizer que o shell serve so para o PRIMEIRO master da instalacao e a nao usar mais e-mail/senha de exemplo. Testes: 15 novos (`MasterAccountsScreenTests` e `MasterPasswordRecoveryTests`) cobrindo master criando master, troca obrigatoria no primeiro acesso do gestor novo, WhatsApp obrigatorio, e-mail unico no sistema inteiro, cliente barrado na tela e no POST, ninguem se desativa/exclui, o ultimo master ativo nunca cai, exclusao so depois de desativar, redefinicao de senha (marcando provisoria e recusando senha curta), gravacao do WhatsApp e o menu; e o codigo de recuperacao saindo pelo WhatsApp do proprio master, pela instancia da empresa padrao, sem envio quando nao ha telefone. Dois testes de menu ja existentes foram atualizados para incluir o botao Gestores. Suite: 329 testes passando.
- MULTIEMPRESA PARTE 4 (MEDICAO): CONSUMO DE IA POR EMPRESA E TELA DE METRICAS DA CARTEIRA. A API Key do GPT e UMA, da plataforma (o master paga a conta), entao o contador de `OpenAiConfiguration` dizia QUANTO foi gasto e nunca QUEM gastou: com varios clientes usando a mesma chave, nao havia como saber se o gasto do mes veio de um cliente grande ou de todos juntos. Decisao tomada com o usuario nesta etapa: por enquanto NAO existe plano, limite nem travamento de tokens — a plataforma MEDE e nao bloqueia nada (se um dia houver teto por cliente, e o consumo do ciclo mensal desta tabela que sera lido). (1) NOVO MODEL `CompanyAiUsage` (migration `0034_consumo_de_ia_por_empresa`): uma linha por empresa e por MES (`company`, `year`, `month` unicos) com `total_requests`, `total_prompt_tokens`, `total_completion_tokens`, `total_tokens`, `first_used_at` e `last_used_at`. Uma linha por mes (e nao uma por chamada) mantem o historico mes a mes sem a tabela crescer com o volume de mensagens, e o "mes atual" reinicia sozinho na virada, sem ninguem zerar a mao. `record()` soma de forma atomica (`F()` + `get_or_create` da linha do mes, seguro para as threads de background da IA) e IGNORA chamada sem empresa; `month_totals()` devolve o mes sempre ZERADO quando nao houve uso (a tela nao trata ausencia), `previous_reference()` da o mes anterior virando o ano e `all_time_totals()` soma todos os meses. (2) `gpt/client.chat_completion` ganhou o parametro OPCIONAL e somente-nomeado `company`: quando vem, o MESMO consumo tambem e somado no contador da empresa, sem mudar nada no envio (chave e modelo continuam da plataforma). Diferente do `wapi/client.py`, aqui a empresa NAO e obrigatoria de proposito: nao ha risco de "falar pelo canal do cliente errado", so de nao medir, e chamadas da plataforma (o `test_connection()` da tela) legitimamente nao tem empresa. Falha no contador da empresa nunca derruba a resposta do GPT (mesmo padrao do contador da plataforma). Quem passa a empresa e `gpt/attendant.py` (`company=conversation.company`) — sem isso a medicao existiria so na API e nunca gravaria nada de verdade. (3) TELA METRICAS DO CLIENTE ganhou o bloco INTELIGENCIA (IA): tokens do mes atual, do mes anterior e acumulado, com chamadas, se a IA esta ligada no primeiro atendimento e a ultima resposta dela (`client_metrics.css?v=2`). (4) TELA NOVA METRICAS DOS CLIENTES (`metricas/`, `platform_metrics_view`, rota `platform-metrics`, `PLATFORM_METRICS_ITEM` no menu do master entre Clientes e Inteligencia (IA)), exclusiva do master via `require_master`: e o "um lugar so" para acompanhar a carteira — resumo da plataforma, a conta da IA e uma LINHA POR EMPRESA (canal configurado + ultimo evento de webhook, modo de primeiro atendimento, conversas ativas/aguardando, mensagens em 30 dias e no total, respostas automaticas, tokens no mes com chamadas e acumulado, ultima mensagem e o botao Detalhes para a tela do cliente). Ordem: empresa ativa primeiro, depois maior consumo de IA no mes e maior movimento, para quem esta pesando na conta aparecer em cima. `build_platform_metrics()` usa consultas AGREGADAS por empresa (uma por assunto, nao uma por cliente), entao a tela nao fica mais lenta conforme a carteira cresce; a saude do canal aqui e so "credencial cadastrada + ultimo evento", porque consultar a W-API de verdade e uma chamada externa POR EMPRESA e continua atras do botao Testar conexao da tela de cada cliente. O total da PLATAFORMA aparece SEPARADO da soma por empresa (e nao somado): ele inclui os testes de conexao, que nao tem empresa, e tudo o que foi gasto antes de existir a medicao por cliente — a tela explica isso num cartao em vez de misturar coisas diferentes. Novos `templates/accounts/platform_metrics.html` e `static/css/platform_metrics.css?v=1` (escopo `.platform-metrics-page`, cartoes que viram coluna unica abaixo de 700px e a tabela rolando SO dentro do proprio painel, sem rolagem horizontal na pagina). A REGRA DE PRIVACIDADE continua intacta: as duas telas mostram apenas numeros e datas — nada de texto de mensagem, contato, grupo, arquivo ou credencial (Instance ID, token, API Key), com teste garantindo. Testes: 33 novos (`CompanyAiUsageModelTests`, `GptUsagePerCompanyTests`, `PlatformMetricsScreenTests`) cobrindo a linha do mes criada/reaproveitada, total calculado quando a API nao manda, uma empresa nunca contando na outra, mes sem uso zerado, mes anterior virando o ano, acumulado somando meses, chamada sem empresa nao gravando nada, consumo apagado junto com a empresa, a chamada real ao GPT contando nos DOIS contadores, o atendente virtual informando a empresa da conversa, master abrindo a tela nova (ADM leva 403, anonimo vai para o login), a tela nunca mostrando conteudo nem credencial, os numeros por empresa e os totais, a ordenacao por consumo, a empresa inativa indo para o fim sem desaparecer, divisorias nao contando como mensagem e o total da plataforma separado da soma por cliente. Tres asserts de menu ja existentes passaram a incluir o botao Metricas.
