# ============================================================================
# BEEZAP — dependencias para o sistema rodar
# ----------------------------------------------------------------------------
# Requer: Python 3.12+
# Instalar os pacotes pip:  pip install -r requirements.txt
# ============================================================================

# --- Pacotes Python (pip) ---
Django==5.2.12
gunicorn>=23.0,<24.0          # servidor WSGI em producao (atras do Nginx)
psycopg[binary]>=3.2,<4.0     # driver PostgreSQL (se usar DATABASE_URL=postgres...)

# ============================================================================
# --- Dependencias de SISTEMA (NAO sao pacotes pip; instalar no S.O.) ---
#
#   ffmpeg   OBRIGATORIO p/ envio de midia:
#            (1) converte o audio gravado no navegador (.webm) para .ogg;
#            (2) converte imagens nao suportadas pela W-API (webp/gif/bmp/heic...)
#                para JPEG.  Sem ele, esses envios FALHAM (o `manage.py check`
#                emite o aviso beezap.W001).  JPG/PNG, video, documento e texto
#                continuam funcionando sem ffmpeg.
#              Linux:  sudo apt update && sudo apt install -y ffmpeg
#              macOS:  brew install ffmpeg
#              Windows (dev): baixar de https://ffmpeg.org e por no PATH
#
#   nginx    proxy reverso + serve /beezap/static/ e /beezap/media/ (producao).
#   git      deploy (git pull).
#
#   Ver docs/DEPLOY.md, docs/DEPLOY_VPS.md e docs/WAPI_MEDIA_INTEGRATION.md.
# ============================================================================
